**/__pycache__/
*.pth
*.safetensors
*.bin
*.pkl

# 虚拟环境
physctrl/

# 数据与输出
src/data_generation/data/
*.h5
!src/data_test/          # 例外:14 个 eval 用 held-out h5 入库,方便 push/pull 同步到服务器
!src/data_test/*.h5
outputs/
checkpoints/
*.csv
# 数据打包不入库(如 diff_E_data.zip,另走 scp/rsync 同步)
*.zip
# 多 E 随机数据(含 h5 与 visualization/*.gif),本地生成不入库
src/diff_E_data/

# 训练产物副本(checkpoint/log/曲线图,不入库)
23(sota)/

# 项目级 Claude 规范,本地用,不入库
CLAUDE.md