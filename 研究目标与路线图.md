# Traceformer 研究目标与路线图

> 本文件是项目当前研究目标、阶段划分和推进条件的唯一完整来源。
> 当前实验执行与结果记录见 `实验记录_1.md`；历史实验见 `实验记录.md`。

## 1. 总体目标

围绕粒子物理仿真，优先研究 Transformer 结构，以及粒子状态、材料参数和其他物理条件的表示与注入方式。目标是在长程 autoregressive rollout 中获得准确、稳定、具备材料可控性的动力学预测，并在此基础上逐步扩展到任意形状边界。

研究工作按三个阶段推进。阶段顺序是约束：一期未达到验收标准前，不把主要训练预算投入任意边界；二期完成后进入集中论文写作和补充实验阶段。

## 2. 一期目标：通用多材质物理仿真

### 2.1 研究范围

一期优先探索：

1. Transformer block 结构和跨层信息流。
2. 粒子状态表示，包括位置、速度、形变和历史运动状态。
3. `E`、`nu`、材质类别及其他物理条件的表示方式。
4. 状态与物理条件的注入位置、注入频率和交互机制。
5. 对长程 rollout 累计误差的结构性控制。

固定地板 contact feature 可以作为已有输入继续使用，但任意边界、复杂障碍物和多物体边界交互暂不作为一期主线。

### 2.2 验收目标

一期完成必须同时满足以下四项，而不是只优化 overall 平均值：

#### A. 长程 rollout 精度可用

- `long_mse`、`FDE` 和逐帧误差增长曲线相对当前强基线有稳定改善，或达到论文可接受水平。
- 不以明显牺牲 short horizon / single-step 精度换取有限的远期改善。
- 结果必须在冻结 test split 上报告，并给出逐材质结果和不确定性。

#### B. 多材质表现均衡

- Plasticine 和 sand 的表现稳定、具有竞争力。
- Elastic 的表现出色，并作为模型优势之一。
- 任一材质显著退化时，overall 指标改善不能直接判定方案成功。

#### C. `E`、`nu` 具有明确的运动控制作用

- 改变 `E`、`nu` 后，预测轨迹和形变产生可辨认、方向合理的响应。
- 不仅证明网络读取了条件，还要通过 counterfactual、sensitivity 或参数交换实验验证输出确实依赖这些条件。
- 分别报告不同材质和不同参数区间的响应，避免均值掩盖条件失效。

#### D. 物理合理性不退化

- 同时检查形状残差、质心漂移、体积、速度/加速度、穿透等指标。
- 穿透率下降但物体运动被过度抑制，不视为真正改善。
- 精度和物理指标冲突时，报告 Pareto trade-off，不用单一指标替代完整判断。

### 2.3 当前方法裁决原则

- 所有新方案必须回答它服务于 A/B/C/D 中的哪一项目标。
- 比较 Transformer 架构时，架构是唯一变量；比较条件表示时，条件表示/注入方式是唯一变量。
- 新方法至少报告 overall、elastic、plasticine、sand 四组结果。
- checkpoint 内 knockout 只用于机制诊断，不能替代从头训练的独立 baseline。
- 负结果完整记录；达到停止条件的路线关闭，不因局部亮点无限追加变体。

### 2.4 当前状态

- A2 串行/并行比较、rollout-aware 训练、速度输出、若干局部/切片注意力和 singleframe 路线已有大量历史结果，见 `实验记录.md`。
- 当前一期强基线冻结为 `mm3_contact_cond@90k`；固定地板 contact condition 作为已验证输入保留，但不在一期继续扩展为任意边界。
- B0/B0.1 已确认：模型明显依赖 `mat_type`，`E` 的作用具有材质差异，`nu` 整体很弱。该结论只说明模型是否使用条件，不等同于反事实预测正确。
- B0.2 已确认数据条件成立：782 个 train 与 41 个 test 对象全部有效，train 内 `E/nu` 无严重静态混杂；三种材质的 GT 对 `E/nu` 均至少在体积应变上存在稳定独立响应，且 `E` 的跨响应作用明显强于 `nu`。Elastic test 仅有两个数值极小的边界越界，不构成整体明显 OOD。模型材料敏感性不足不能再归因于数据完全无信号，但该 observational audit 不证明 counterfactual 因果正确。
- B1a/B1b 已检验 hybrid-state feedback：全局状态反馈存在局部收益，但与 contact 组合后产生明显跨材质冲突。B1b gate knockout 的简单 dynamic-gate 预注册判定为 `close`，不再回到 v11a 继续追加变体。
- B2 已在冻结的 `mm3_contact_cond@90k` 上完成连续 `E/nu` 扫描，判定为 `revise`：`E` 的有效响应主要集中于 sand，`nu` 未形成普遍、稳定的连续控制，目标 C 尚未达成。
- B3a/B3b 已完成：Joint Material-State Adapter 虽显著改善 sand，但伤害 elastic/plasticine 和穿透；后验 material-stage scalar gate 几乎全部回到 identity，无法修复该失衡。B3 adapter 家族按预注册判定 `close`，不再调 rank、expert、gate 或注入 stage。
- B4 Continuous Material AdaLN 已完成 45k 筛选并判定 `close`：新路径在 ON/OFF knockout 中明显承重，sand long 改善 25.85%，但 plasticine long/FDE 分别恶化 294.43%/110.28%，地面穿透增加 2.022 pp，Gate A 失败。按预注册未运行材料 sweep，也未继续至 90k。
- 当前重新回到冻结的 `mm3_contact_cond@90k` 独立 baseline。B0.3 已在冻结 41-model factual test 上完成：速度、加速度和质心响应整体保真，但 sand 的整体形状变化和几何体积变化明显被压缩；其中 GT 的 `nu -> centered_shape_mse` partial Spearman 为 `+0.644`（95% CI `[0.102, 0.879]`），prediction 为 `-0.033`，而 sand 的凸包体积变化也被明显低估。该结果说明模型可能丢失 `nu` 对形变的事实响应，但 centered-shape 与凸包仍是全局几何量，不能直接定位本构误差。当前注册 B0.4：先在 train calibration subset 用真实 `F/J` 验证位置局部形变估计器，再审计同一 41-model test 的 prediction-GT 局部应变响应；B0.4 不训练模型，校准未通过时禁止作本构解释。后续机制仍必须避开 B3/B4 已连续暴露的“共享条件更新方向牺牲部分材质”问题。

当前证据链和推进顺序固定为：

```text
B0/B0.1：条件依赖与分量诊断
    -> B1a/B1b：状态反馈与跨材质冲突诊断
    -> B2：连续 E/nu 响应与方向诊断
    -> B3a：共享 Joint Material-State Adapter（mixed negative）
    -> B3b：后验 material-stage 标量校准（close）
    -> B4：连续材料 AdaLN 逐层调制（路径承重，但材料失衡，close）
    -> B0.2：数据材料可辨识性通过，GT 信号以体积/应变为主
    -> B0.3：冻结 baseline 的预测-GT response fidelity 审计（完成；sand nu 形变响应丢失候选）
    -> B0.4：train F/J 校准 + test 局部形变响应保真度审计（已实现，等待服务器运行）
    -> 三分流：局部材料响应丢失 / rollout 稳定性 / B0.3 全局统计伪影
    -> 下一机制：只按对应证据分支重新预注册，不预设必须修改 condition encoder
```

下一机制进入完整训练前，必须在 `实验记录_1.md` 中冻结机制假设、唯一变量、基线、成功标准和停止条件。B2.1/v11a 横向扫描不再阻塞主线；除非出现新的、与当前问题直接相关的机制假设，否则不回到已关闭的 HST/B3 adapter 家族，也不对 B4 做 hidden dim、注入层数或学习率变体。

## 3. 二期目标：任意形状边界

一期验收后，研究从固定地板扩展到任意形状边界：

1. 用边界点云、法向、SDF 或局部几何描述替代单一 floor scalar。
2. 建模 particle-boundary interaction、法向接触和切向摩擦。
3. 支持斜面、曲面、容器和一般障碍物。
4. 验证未见边界形状上的泛化，而不只是在训练边界上拟合。
5. 在单材质复杂碰撞场景稳定后，再评估同场景多物体或多材质扩展。

二期不得通过破坏一期的长程精度、材料控制能力或多材质表现来换取边界适配。

## 4. 三期目标：论文与必要补充实验

二期完成后集中完成论文主体，并补足：

- 主 baseline 和相关 neural simulator 对比。
- 参数量、FLOPs、显存和推理速度。
- 核心模块消融和注入位置消融。
- 多 seed 与置信区间。
- `E/nu` 插值、外插和反事实控制实验。
- 分材质长程误差曲线和失败案例。
- 任意边界泛化实验。

论文写作素材、实验台账、方法定义和图表从一期开始持续维护；“三期写作”指集中成稿，不表示此前不记录。

## 5. 文档职责

- `研究目标与路线图.md`：当前目标、阶段和验收条件的唯一完整来源。
- `实验记录_1.md`：一期当前实验的唯一主台账。
- `实验记录.md`：一期以前及已关闭路线的历史实验归档。
- `phase1-5_roadmap.md`：旧 Phase 1–5 规划归档，不再决定当前优先级。
- `AGENTS.md`：工程执行规则及上述文档关系的简要约束。
