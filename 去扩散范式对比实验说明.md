# 去扩散范式对比实验说明 — 确定性回归 vs 原版 PhysCtrl 扩散(2048 粒子数据)

> 2026-07-13 定稿。本文档是两个实验(**diffE2048 确定性臂** + **physctrl-orig 扩散臂**)的完整说明:动机、设计、机制细节、代码修改、评测口径、结果、分析、结论。
> 量化结论以 `实验记录.md`(diffE2048 节 / physctrl-orig 节)为准,本文档为展开版;两者如有出入,以台账为准并修本文档。

---

## 0. TL;DR

**同数据、同架构、同 90000 步、同卡下,原版 PhysCtrl 扩散范式与本项目确定性回归的误差轮廓互补:确定性 short/mid 段全胜(f5 ×32、mid −6.7%),扩散 long 段小胜(f18-24 −14.3%),交叉点精确在 f15→f16。跨帧总结依赖聚合权重:算术均值打平(−3.0%,晚帧主导),几何均值确定性好 1.67×(§8.2/§7.3)——论文只用逐帧曲线下结论。Procrustes 分解(§7.3/§8.4):位姿漂移两臂相当,形状保真确定性全程 2~4× 更好,扩散晚帧优势仅在可对齐位姿分量;失效形态 = 确定性轻度均匀膨胀 vs 扩散核心收缩+外壳外溢。扩散训练贵 1.73×(实测)、推理贵约一个量级(估算),地面穿透恰好为 0 是其唯一物理赢项。**

论文可写的主张:**「在确定性仿真数据上,确定性回归以约 1/10 的推理成本达到不差于扩散的总体精度,且短时/早段保真显著更优」**。
论文不能写的主张:「确定性全面碾压扩散」——f20 之后扩散更准,这一点必须如实呈现(反而可以做成「何时需要扩散」的边界刻画 hook)。

这组实验解除了论文/申博路径上最大的阻塞项:「去扩散」此前只有断言、没有同条件实验。

---

## 1. 背景与动机

### 1.1 论文主张与它欠的证据

本项目(Traceformer)对 PhysCtrl(NeurIPS 2025)的核心改动是**去扩散**:把「DDPM 训练 + DDIM 采样一次生成整段轨迹」换成「确定性回归 + 单帧自回归 rollout」。

原理依据:训练数据由 MPM 仿真生成,**仿真超参全部固定** → 给定初始点云 + 物理条件 c,后续轨迹 P 是确定的,p(P|c) 退化为确定映射 → 建模一个分布(扩散)是多余的,直接回归即可,且省掉多步采样的推理成本。

问题在于:这个主张此前**只有原理论证,没有同条件对照实验**。审稿人第一个问题必然是「你和原版扩散在同一数据上比过吗」。这就是本组实验。

### 1.2 为什么在 2048 粒子数据上比

- 2048 是**原版 PhysCtrl 的粒子数**(上游 config pc_size=512 是其发布 config,但其数据管线/物理 loss 网格按 2048 设计;本项目此前主线在 512)。在 2048 上比,排除「你把原版降配了」的质疑。
- 2048 数据(`diff_E_2048_data`)是现成的:E/nu 随机、254 train / 13 test、held-out 物体协议干净。
- 确定性臂(diffE2048)本来就要训——它同时承担「方法 scale 到 2048 粒子」的验证任务,一臂两用。

---

## 2. 实验设计总览

### 2.1 两臂一览

| | 臂 A:diffE2048(确定性) | 臂 B:physctrl-orig(扩散) |
|---|---|---|
| config | `config_diffE2048_singleframe_geom_deform_d0001.yaml` | `config_diffE2048_physctrl_orig.yaml` |
| eval config | `eval_diffE2048_singleframe_geom_deform_d0001.yaml` | `eval_diffE2048_physctrl_orig.yaml` |
| 范式 | 确定性回归,单帧自回归 rollout | DDPM 训练,DDIM 50 步一次生成整段 |
| 输入/输出 | 5 GT 帧条件 → 1 帧 | 1 GT 帧条件 → 24 帧(整条轨迹) |
| 损失 | xyz + sfG 几何正则(lap 0.5 / edge 1.0 / collision 0.1 / floor 0.1)+ 单帧 boundary loss_F(λ=0.001) | 原版:xyz + λ_vel=1.0 + 多帧 MPM loss_F(λ_deform=0.001),几何正则全 0 |
| 推理 | 20 次前向(每帧 1 次,喂回) | 1 次 DDIM 采样(50 步去噪) |
| output_dir | `outputs/diffE2048_singleframe_geom_deform_d0001_8L` | `outputs/diffE2048_physctrl_orig_8L` |

### 2.2 冻结项(两臂完全一致)

- **数据**:`diff_E_2048_data/2048_data/2048_train`(254 个 h5,uid 30xxx 段;每条 25 帧 × 2048 粒子 × 3;E/nu 随机)。测试 `2048_test`(13 个 h5,uid 39xxx 段,与训练物体**零重叠** = held-out 物体)。
- **架构**:串行 `SpatialTemporalTransformerBlock`,8 层 / latent 256(≈16M,与 run23 同架构);`mask_cond: true` / `floor_cond: true`。
- **训练预算**:90000 步、lr 1e-4(constant_with_warmup 100)、bf16、grad ckpt + xformers、有效 batch = 1×8 accum = 8、单卡 5090。
- **评测**:同一测试集、两臂均取 checkpoint-90000(训练终点)、eval seed=0。

### 2.3 「唯一变量」的诚实说明

这**不是单变量实验**,是**范式级捆绑对比**:换范式必然连带换(a)条件帧数 1 vs 5、(b)one-shot vs 自回归、(c)损失组(原版 loss vs sfG+loss_F)。三者无法在保持「各自范式的最优实践」的前提下拆开。论文里如实写 paradigm comparison,每臂用各自范式的代表性配置:扩散臂 = 上游原版配方,确定性臂 = 本项目 SOTA 配方。

与上游 base config 的两处**显式偏离**(写在 config 头注里):
1. `mask_cond/floor_cond: true`(上游 base 为 false/false)——与 run23 及本项目全部臂一致,保证条件通道跨臂可比;
2. 单卡有效 batch 8(上游 8 卡 ×8 accum = 64)——硬件所限;两臂同 batch,内部对比公平,但相对上游属欠训(见 §7 caveat)。

---

## 3. 臂 A:diffE2048 确定性臂(细节)

### 3.1 方法栈(= 项目 SOTA 配方,sfG+loss_F λ=0.001)

`config` 完全复制 diffE-512 臂(`config_diffE_singleframe_geom_deform_d0001.yaml`),只改 3 处:output_dir、`pc_size: 2048`、dataset_path。方法链条的由来(详见台账对应节):

- **单帧自回归(sf)**:`output_frames: 1`,input 5 帧,推理时逐帧喂回。full-rollout −9.7% vs run23,但体积漂移 +69% → 衍生 sfG。
- **sfG = sf + 几何正则**:`lambda_laplacian: 0.5`(曲面平滑)+ `lambda_edge: 1.0`(边长保持)+ collision 0.1 / floor 0.1。full-rollout 再 −22.7%。
- **+ 单帧 boundary loss_F**:`single_frame_deform: true` + `lambda_deform: 0.001`(经 step-2000 校准,deform/xyz 比值 0.33× 命中拐点)。512 数据上 full-rollout 2.978e-3 = 项目最低。
- **训练窗口**:`rollout_random_window: true` + `windows_per_model: 20`(每 model 每 epoch 20 个随机起点窗口)。

### 3.2 训练调度(2048 适配)

- `max_train_steps: 90000`(512 臂为 60000;2048 臂延长,commit `2b7d5ac`)。
- `validation_steps: 2000`(原 500;2048 粒子空间注意力 O(N²),val 前向贵 ~16×,降频省时)。
- `checkpoints_total_limit: 19`(保住 checkpoint-45000 对比点;最终两臂都到 90000,45000 点实际未用)。

### 3.3 已知混淆(config 头注原文)

- 与 diffE-512 跨臂比带「物体集不同」混淆(30xxx vs 20xxx 零重叠)→ 2048 vs 512 的采样密度结论须另做同物体集对照;**但本文档的范式对比不受此影响**(两臂同用 30xxx)。
- Chamfer/凸包体积统计量随采样密度移动,跨 N 对比谨慎;两臂同 N=2048,内部可比。

---

## 4. 臂 B:physctrl-orig 扩散臂(细节)

### 4.1 原版范式在本代码库如何还原(全部走 config,无新代码路径)

上游 cwchenwang/physctrl 的 train.py **没有** INPUT_FRAMES/OUTPUT_FRAMES/use_diffusion 这些开关——「5 帧入 5 帧出的 chunk 化确定性回归」整个是 physctrl_2 的加法。原版模式 = 数据给「首帧 + 24 帧目标」,DDPM 加噪训练,DDIM 一次生成。physctrl_2 里这些机制都保留且 config 化了:

| 原版行为 | 本库还原方式 | 机制位置 |
|---|---|---|
| DDPM 1000 步,`prediction_type='sample'`,clip_sample=False | `use_diffusion: true` | train.py:498-499, 535-539 |
| 首帧条件 + 一次生成 24 帧 | `input_frames: 1` + `output_frames: 24` | train.py:293/317-318 |
| 每条轨迹一个训练样本(无 chunk 滑窗) | 窗口跨度 = (1+24−1)×1+1 = 25 = 全轨迹 → max_start=0,每 model 每 epoch 恰 1 个 start=0 窗口 | traj_dataset.py 窗口枚举 |
| 无速度输入通道 | start_idx=0 → start_vel 自动为零向量(信息等价) | traj_dataset.py:276-277 |
| 原版 loss | `lambda_vel: 1.0` + `lambda_deform: 0.001`(多帧 MPM 残差,走 `DeformLoss.forward`),`lambda_mask/momentum/laplacian/collision/edge/floor: 0` | train.py:581/597-611 |
| DDIM 采样推理 | eval:`use_diffusion: true` + `num_inference_steps: 50`(pipeline 上游默认值);output=24 → ROLLOUT_STEPS=ceil(20/24)=1,一次采样无喂回 | eval.py:92-96/106, pipeline_traj.py |
| 位置嵌入适配 26 帧(24 输出+1 条件+1 mask) | MDM_ST 按 `cond_frames` 自动定 sample_frames,sincos buffer 非学习参数 | spacetime.py:2446/2458/2508 |

eval 约定的一处**显式例外**:项目规范要求「评测必须 use_diffusion:false + num_inference_steps:1」,那是针对确定性臂的推理路径一致性;本臂被评测的**就是**扩散范式本身,故 eval 用 DDIM 50 步(已写入 eval config 头注)。

### 4.2 训练调度

与臂 A 相同(90000 步、val 2000 等)。验证成本注意:每次 validation = 50 步 DDIM × 8 个 val model,显著比确定性臂贵。

---

## 5. 为跑通所需的代码修改(仅一处 blocker,已修)

**`src/utils/physics.py` 的 `DeformLoss` 粒子数 N=512 硬编码族**。这是 physctrl_1 阶段的手改(physctrl_o 原始克隆中 N 仍是上游的 2048,可证),在 2048 粒子数据上 reshape 必崩:

| 方法 | 触发路径 | 修复 commit |
|---|---|---|
| `forward_single_step` | 臂 A 的单帧 loss_F(训练时实际崩过,服务器 traceback 定位) | `fa745ac` |
| `forward`(多帧向量化) | **臂 B 的原版 loss_F 必经路径**(train.py:608) | `34ade6d` |
| `forward_sequential` | 不在训练路径,顺手修 | `34ade6d` |

修法统一:`N = x.shape[2]` 从输入形状推导,`self.N`/`self.I33` 成员删除,单位阵改为局部 `torch.eye(3)` 广播。

**验证(本机 CPU,physctrl env)三层全过:**
1. `python -m py_compile` 通过;
2. 数值回归:N=512 时新旧实现 loss **逐位相等**(6.1832481623e-01),N=2048 时 `forward`/`forward_sequential` 输出有限;
3. dry-run:dataset(250 个 start=0 全长窗口、points_tgt (24,2048,3)、start_vel 全零)+ model(cond_frames=1,24 帧加噪输入 + 随机 timestep → 输出 (1,24,N,3) 有限)。

---

## 6. 评测协议与口径(跨臂比较的陷阱,必读)

### 6.1 窗口结构不同

- 臂 A(output=1,input=5):测试集枚举 4 个起点窗口/model → **52 窗口**(13 model × 4),其中 start=0 的 13 个是 full-horizon(rollout 指标分母)。
- 臂 B(output=24):只有 start=0 一种窗口 → **13 窗口**,全部 full-horizon。

### 6.2 full-rollout / first-chunk 跨臂不可直接比

- `MSE full-rollout` 的分母**含零误差的条件 GT 帧**,且两臂含的帧数不同:臂 B 含 1 帧(自洽验证:first-chunk 3.2619e-3 × 24/25 = 3.1314e-3 = full-rollout,严格成立),臂 A 含 5 帧。
- `MSE first-chunk` 语义完全不同:臂 A = 单步预测(6.43e-6),臂 B = 整段 24 帧一次生成(3.26e-3)。**放在一张表里毫无意义。**
- 臂 B 的 `MSE per step` 行(step1=1.147e-2)与聚合 per-step(n=0)是 output=24 下的记账 quirk,**不可用**。

### 6.3 唯一干净的跨臂口径

1. **MSE per-abs 对齐帧(f5/f10/f15/f20)**:两臂都在 start=0 轨迹上按绝对帧号对齐,逐点 MSE 量纲一致 → **主对比口径**;
2. **Chamfer**(同 N=2048);
3. **物理合理性指标**(vMSE/aMSE/体积超额自漂移/穿透)——其中体积看**超额**(pred 自漂移 − GT 自漂移),GT 凸包自漂移本身非零(本测试集聚合 10.89%)。

### 6.4 其他口径事项

- 臂 A 的 `loss_F (pred/gt)` 打印为 0 是**设计跳过**(eval.py:200-208,多帧 MPM 残差需 ≥3 帧,output=1 不适用),非异常。
- 臂 B 的扩散采样有随机性,eval 由 seed=0 固定、单次可复现;**严格讲扩散指标应多 seed 平均**(见 §9 待办)。

---

## 7. 结果

### 7.1 主对比表(2048_test,n=13,两臂均 checkpoint-90000;Δ = 扩散 vs 确定性)

| 指标 | 臂 A 确定性 | 臂 B 扩散 | Δ | 判读 |
|---|---|---|---|---|
| per-abs f5 | 4.802e-6 | 1.547e-4 | **×32.2** | 确定性早段大胜 |
| per-abs f10 | 5.487e-4 | 1.061e-3 | **+93.3%** | 确定性胜 |
| per-abs f15 | 2.918e-3 | 3.009e-3 | +3.1% | 平 |
| per-abs f20 | 7.970e-3 | 6.877e-3 | **−13.7%** | 扩散晚段小胜 |
| 对齐 4 帧算术均值 | 2.860e-3 | 2.775e-3 | −3.0% | ⚠️ 晚帧主导(f15+f20 占和的 ~90%+),见 §8.2 |
| 对齐 4 帧几何均值 | **4.98e-4** | 1.36e-3 | **确定性 2.7× 更好** | 等权看各帧**相对**误差,见 §8.2 |
| Chamfer | 5.189e-2 | 5.450e-2 | +5.0% | 确定性略好 |
| vMSE(帧差速度) | 9.474e-5 | 7.594e-5 | −19.8% | 扩散好 |
| aMSE(二阶差) | 6.439e-6 | 8.976e-6 | +39.4% | 确定性好 |
| 体积相对误差 vs GT | 4.84% | 14.16% | — | 确定性好 |
| 体积超额自漂移(聚合) | −0.97% | −1.61% | — | 均近零(都有早负晚正抵消) |
| 体积超额 f10 → f24 | −3.59% → **+3.95%** | −9.70% → **+10.01%** | — | 扩散摆幅大:早段更欠形变、**晚帧发胖 2.5×** |
| 穿透率 / 深度 | 0.423% / 8.85e-4 | **0.000% / 0** | — | 扩散完美(全项目唯一) |
| loss_F(pred / GT-ref) | —(跳过) | 3.151e-1 / 6.168e-2(**5.1×**) | — | 扩散生成的 MPM 一致性显著劣于 GT |

参考(不入对比,口径不同):臂 A full-rollout 3.363e-3 / first-chunk 6.43e-6;臂 B full-rollout 3.131e-3 / first-chunk 3.26e-3。

### 7.2 成本(同卡 5090、同 90000 步、同有效 batch 8)

| | 臂 A 确定性 | 臂 B 扩散 | 比值 |
|---|---|---|---|
| 训练 wall-clock(实测) | 25h24m(≈1.02 s/it) | 44h03m(≈1.76 s/it) | **1.73×** |
| 推理(**估算**,未实测) | 20 次前向 × 6 帧注意力 | 50 步 DDIM × 26 帧注意力 | **~10×** |

训练比值(1.73×)低于纯 FLOPs 粗估(26 帧 vs 6 帧注意力 ~3.7×),因为:扩散臂省掉了 sfG 的 kNN 几何正则开销;数据加载/优化器等固定成本摊薄差距。
⚠️ 推理的 ~10× 是估算——**论文引用前必须实测**(两臂 eval 各记一次单 model 生成耗时即可)。

### 7.3 分解指标(度量层扩展 @90k 重评,n=13;commit `8964433`,原始输出见 results_diffE2048_*.md)

分段 / 聚合 / 端点(粗体 = 更好):

| 指标 | 臂 A 确定性 | 臂 B 扩散 | 判读 |
|---|---|---|---|
| seg-MSE short(f5-10) | **1.921e-4** | 5.509e-4 | A 2.9× 好 |
| seg-MSE mid(f11-17) | **2.466e-3** | 2.644e-3 | A −6.7%(4 锚点表看不出:mid 其实 A 赢) |
| seg-MSE long(f18-24) | 9.407e-3 | **8.061e-3** | B −14.3% |
| 交叉点 | — | — | 全帧曲线精确定位在 **f15→f16** |
| GM-MSE(f5-24) | **1.324e-3** | 2.216e-3 | A 1.67× 好 |
| MSE@f24(端点) | 1.349e-2 | **1.151e-2** | B −14.7%(f21-24 补齐,B 全段保持) |
| FDE(末帧逐点 L2) | 1.560e-1 | **1.489e-1** | B 仅 −4.5% ← 远小于 MSE 口径 → A 误差更长尾 |

Procrustes 位姿/形状分解(f24;逐锚帧完整数据见 results 文件):

| 分量 | 臂 A | 臂 B | 判读 |
|---|---|---|---|
| 质心偏移 | 5.09e-2 | 4.86e-2 | **相当** |
| 旋转 | 33.5° | 34.4° | **相当** |
| 尺度 s | 0.969(pred 偏大 3.1%) | 1.024(pred RMS 半径反而偏**小** 2.4%) | 方向相反 |
| 残余形状 MSE | **1.495e-3** | 3.144e-3 | **A 2.1× 好**(f5~f24 全程 2~4×) |
| 可对齐位姿分量(总−残余) | 1.20e-2 | **8.4e-3** | B 的晚帧 MSE 优势全在此 |

固定边弥散度(f24 超额 vs GT):A **+4.26%**(边长膨胀)vs B **−4.33%**(边长收缩)——幅度几乎相同、方向相反。预期的 Goodhart 偏向(A 训过 lambda_edge 会占便宜)未发生。
⚠️ 解读 caveat:Umeyama 对非刚体形变是投影,「旋转/尺度」会部分吸收形变;但同帧同 GT 下跨臂比较公平。

---

## 8. 分析

### 8.1 误差轮廓互补的机制(本对比最重要的发现)

- **确定性臂是自回归**:早段有 5 GT 帧条件 + 短程外推,误差极小(f5 = 4.8e-6),但预测喂回导致误差**复合累积**——f5→f20 增长 ×1660。
- **扩散臂是 one-shot**:没有喂回,不存在复合;误差随帧增长只反映**本征难度**(远帧不确定性大)——f5→f20 仅 ×44。起点高(一次生成 24 帧,早帧也做不到 AR 那么准)、斜率缓,到 f20 反超。
- 这是「AR 误差累积 vs 一次性生成」的教科书 trade-off,本数据上交叉点由全帧曲线精确定位在 **f15→f16**(§7.3)。这个交叉点本身有论文价值:它给「什么 horizon 之内确定性 AR 是对的选择」一个量化边界,可沿仿真随机性/horizon 两轴扩展成「何时需要扩散」的受控刻画(申博 hook 候选,见 memory `physctrl2-publication-goal`)。
- ⚠️ 分解测量(§8.4)对此机制叙事的修正:「无复合优势」只体现在**可对齐位姿分量的分布**上(A 长尾),不体现在位姿漂移均值上(两臂相当);且 B 的形状轴全程更差——「one-shot 更稳」是分量级、非全面的。

### 8.2 单标量聚合的权重敏感性(「打平」结论的适用边界)

误差从 f5 到 f20 跨 3 个数量级 → **任何跨帧单标量都隐含一个 horizon 权重选择**:
- **算术均值**(−3.0%,B 略好):和被最大项主导——f15+f20 占 A 总和 95%、B 89%,f20 单项占 62~70%。它回答「全程绝对误差」,早段 ×32 的优势(绝对量 1.5e-4)敌不过晚段 16% 的劣势(绝对量 1.1e-3)。
- **几何均值 / log-MSE 平均**(全帧 f5-24 实测:A 1.324e-3 vs B 2.216e-3,**A 好 1.67×**;早期仅用 4 锚点粗算为 2.7×,以全帧为准):每帧等权看相对误差,早段优势不被绝对尺度淹没。

同一组数字,两种聚合结论相反 → **「总体打平」严格说是「算术均值口径下打平」**。论文呈现必须用逐帧曲线 + 交叉点,不用单标量下总结论;若下游在乎短时精度(控制/交互),几何均值口径(确定性明显占优)才是对的问题。

### 8.3 早段 32× 里有多少是「信息不对等」

确定性臂条件 = 5 GT 帧(隐含速度/加速度),扩散臂 = 1 GT 帧 + 零速度向量。f5 的 ×32 不能全记给范式——一部分是条件信息差。诚实写法:早段优势 = 「AR 短程外推 + 更厚的条件窗」的联合效应;f15~f20 的反超则是纯范式效应(那时确定性臂的 GT 信息早已稀释,扩散臂的无复合优势显形)。

### 8.4 位姿/形状失效轴:分解测量结果(修正版;原推断半对半错)

晚帧的表面矛盾(A 的 MSE 更差但体积超额更好)最初被推断为「A 位置错形状好 / B 位置对形状坏」。Procrustes 分解实测(§7.3)后,**这个图像一半被证实、一半被证伪**:

- **✅ 证实:「B 形状糊」**——对齐后残余形状 MSE,A 全程好 2~4×(f24:1.50e-3 vs 3.14e-3)。形状轴 B 全程输,与 loss_F 残差 5.1× GT 相互印证。
- **❌ 证伪:「B 位置对」**——f24 位姿漂移两臂相当(质心 5.09e-2 vs 4.86e-2、旋转 33.5° vs 34.4°)。**位姿漂移是任务本征难度,不是 AR 复合专属**;one-shot 一样漂。
- **B 的晚帧 MSE 小胜从哪来?** 可对齐位姿分量(总−残余):B 8.4e-3 vs A 12.0e-3。逐项均值相当而分量贡献不同 → A 的位姿误差在窗口间更长尾/方差更大(FDE 只差 4.5% vs MSE@f24 差 14.7%,平方口径放大长尾,佐证)。逐 model 定位靠 per-model CSV,待查。
- **失效形态相反,且都不是当初想的样子**:
  - **A = 轻度均匀膨胀**:尺度 s=0.969(偏大 3.1%)、固定边 +4.3%、凸包 +4.3%,三口径一致——带着 lambda_edge=1.0 训练也没箍住 rollout 累积膨胀;
  - **B = 核心收缩 + 外壳外溢**:s=1.024(RMS 半径反而**小** 2.4%)、固定边 −4.3%(局部聚拢)、凸包 +10%(外包络变大)——多数点向心聚拢、少数点外溢/各向异性伸展撑大凸包。「发糊」的精确形态是**密度重分布**,不是均匀发胖;像生成模型「回归均值 + 采样噪声晕」的组合。

旁证:A 的 Chamfer 反而更好(−5%)——Chamfer 不看逐点对应、对外溢点双向罚,B 靠位姿分量在 MSE 上占的便宜在 Chamfer 口径吐回。sf 臂「MSE 赢、体积/视觉输」教训(MSE 非充分质量代理)在此镜像重现;**位姿长尾 vs 形状糊哪个视觉更伤,渲染裁决**(形状糊通常更刺眼)。

### 8.5 「扩散更物理」不成立,但穿透是真赢项

- 扩散臂自身的多帧 loss_F 残差 = 5.1× GT 参考(尽管它带着 λ_deform=0.001 训练)→ 生成轨迹的 MPM 形变一致性显著差于 GT;
- 晚帧体积超额 +10.01%(确定性 +3.95%)→ 晚帧形状失真更重(分解后精确形态 = 核心收缩 + 外壳外溢,见 §8.4);
- 但**地面穿透恰好 0**(确定性 0.42%)——全项目唯一穿透为零的臂。一次性生成整段可能更容易全局满足边界约束,而 AR 逐帧误差会把点推过地面。这一点确定性臂应该反思(穿透是 sf 族痼疾,run23 也非零)。

### 8.6 相对上游的欠训 caveat

上游训练见样本 ≈ 60000 步 × 有效 batch 64 = 384 万;本复跑 ≈ 90000 × 8 = 72 万(约 1/5)。扩散通常比回归更吃数据/步数,**不能排除扩散臂在更大预算下更强**。但两臂同预算,内部对比公平;论文如实标注即可。

---

## 9. 结论、Caveat、后续

### 9.1 结论(论文口径)

1. **总体精度不输**:算术均值口径打平(−3.0%,在历史 bf16 rollout 抖动 ±7% 带内且晚帧主导),几何均值口径确定性好 1.67×(§8.2/§7.3)——去扩散**不掉精度**,但「打平/占优」取决于 horizon 权重,论文用逐帧曲线呈现、单标量必须注明聚合方式;
2. **成本**:训练 1.73×(实测)、推理约一个量级(待实测)——去扩散**显著更便宜**;
3. **短/中时保真**:short 段确定性 2.9×、mid 段 −6.7% 全胜——对交互/控制类下游(需要准确短期预测)确定性显著更优;
4. **长时**:long 段(f18-24)扩散小胜 14.3%,交叉点 f15→f16——「全面碾压」不成立,如实呈现并转化为「何时需要扩散」的边界讨论;
5. **物理合理性**:互有胜负(穿透 0 vs 体积/loss_F 更差),不支持任一方的「更物理」叙事;
6. **分解轴(§7.3/§8.4,论文新增可写点)**:位姿漂移两臂相当(本征难度);**形状保真确定性全程 2~4× 更好**;扩散的长时优势仅在可对齐位姿分量(且 A 侧是长尾问题,FDE 只差 4.5%);失效形态 = 确定性轻度均匀膨胀 vs 扩散核心收缩+外壳外溢——「扩散晚段小胜」是分量级、非全面的。

### 9.2 Caveat 清单(引用数字时须带)

- n=13(< 项目 n≥14 硬引用线;测试集全量只有 13,扩测试集才能解决);
- 扩散臂单 seed(DDIM seed=0)单次采样;
- 条件信息不对等(1 vs 5 GT 帧);
- 范式级捆绑对比,非单变量;
- 相对上游欠训(样本见次 ~1/5);
- 推理 ~10× 是估算未实测。

### 9.3 后续(按优先级)

0. ~~度量层 patch~~ ✅ 已实现(`utils/eval_metrics.py` + eval.py 增量输出,单测全过):全帧 per-frame + 分段 seg-MSE(short/mid/long)+ GM-MSE + FDE(修 §8.2 单标量问题)、Procrustes 位姿/形状分解(把 §8.4 的「位置漂 vs 形状糊」变成硬数字)、固定边弥散度(诊断)。**两臂 checkpoint-90000 重跑一次 eval 即可出分解数字**,现有指标逐字节不变;
1. **多 seed DDIM 采样**(2~3 个 seed 平均)硬化扩散臂数字——当前最大口径缺口;
2. **实测推理耗时**(两臂 eval 各记单 model 生成时间),替掉 ~10× 估算;
3. 渲染/视觉对比(交叉点附近谁的轨迹视觉更好,MSE 说了不算——sf 族有「MSE 赢视觉输」前科);
4. 论文图:per-abs 误差曲线(det 早低晚陡 / diffusion 平缓,交叉点标注)+ paradigm comparison 表 + 成本列;
5. (可选)扩散臂 @45000/@60000 中间 checkpoint eval,看扩散是否仍在涨(欠训 caveat 的低成本探针)。

---

## 10. 复现

### 10.1 命令(服务器 5090,从 `src/` 目录)

```bash
# 臂 A 训练
accelerate launch --config_file configs/acc/1gpu.yaml train.py --config configs/config_diffE2048_singleframe_geom_deform_d0001.yaml
# 臂 B 训练
accelerate launch --config_file configs/acc/1gpu.yaml train.py --config configs/config_diffE2048_physctrl_orig.yaml
# 评测(resume 指向 checkpoint-90000)
python eval.py --config configs/eval_diffE2048_singleframe_geom_deform_d0001.yaml
python eval.py --config configs/eval_diffE2048_physctrl_orig.yaml
```

注:repo 里两个 eval config 的 `resume` 写的是 checkpoint-45000(当初定的对比点);实际评测时在服务器上改为 checkpoint-90000。两臂最终对比点 = **90000**,45000 作废。

### 10.2 相关 commit

| commit | 内容 |
|---|---|
| `fa745ac` | `forward_single_step` 粒子数 N 从输入形状推导(臂 A 2048 训练崩溃修复) |
| `b2247a3` | 臂 A config 对齐实际数据路径 + 物体集 caveat |
| `2b7d5ac` | 臂 A 90000 步 + val 降频 2000 + ckpt limit 19 |
| `34ade6d` | 臂 B config 对(train+eval)+ `DeformLoss.forward` 多帧去 N=512 硬编码 |
| `a085631` | 台账补 diffE2048 记录 |
| `20e72ea` | 台账补 physctrl-orig 范式对比记录 |
| `1977757` | 台账补训练成本实测 |
| `8964433` | eval 度量层扩展(`utils/eval_metrics.py`:Procrustes 分解/固定边弥散度/全帧 seg-MSE/GM/FDE) |
| `108e954` | 分解测量结果入档(位姿相当/形状轴 A 胜/B=核心收缩+外溢/交叉点 f15→f16) |

### 10.3 文件清单

- 训练 config:`src/configs/config_diffE2048_singleframe_geom_deform_d0001.yaml` / `src/configs/config_diffE2048_physctrl_orig.yaml`
- 评测 config:`src/configs/eval_diffE2048_singleframe_geom_deform_d0001.yaml` / `src/configs/eval_diffE2048_physctrl_orig.yaml`
- 关键代码:`src/utils/physics.py`(DeformLoss N 推导)、`src/train.py`(use_diffusion / input_frames 分支)、`src/dataset/traj_dataset.py`(窗口枚举 / start_vel)、`src/eval.py`(ROLLOUT_STEPS / per-abs / 物理指标)、`src/utils/eval_metrics.py`(分解指标)、`src/pipeline_traj.py`(DDIM 采样)
- 台账:`实验记录.md` §4 总览表 + §5「diffE2048」「physctrl-orig」两节
- 原始 eval 输出:`results_diffE2048_singleframe_geom_deform_d0001.md` / `results_diffE2048_physctrl_orig.md`(本地工作稿,未入库)

---

## 附录 A · 完整数据对照表(两臂均 checkpoint-90000,2048_test,n=13)

约定:**Δ = (B−A)/A,正号 = 扩散更差**(误差类指标)。粗体 = 该行更好的一方。

### A.1 元信息与不可比指标(仅存档)

| | 臂 A 确定性 | 臂 B 扩散 |
|---|---|---|
| 窗口 | 52(13 全程 + 39 start>0) | 13(全部 start=0 全程) |
| 推理方式 | 20 次前向逐帧喂回 | 1 次 DDIM 50 步采样 |
| MSE first-chunk | 6.430e-6(单步语义) | 3.265e-3(整段 24 帧语义) |
| MSE full-rollout | 3.371e-3(分母含 5 GT 帧) | 3.135e-3(分母含 1 GT 帧) |

### A.2 全帧 per-frame MSE(仅预测帧)

| 帧 | A | B | Δ | 帧 | A | B | Δ |
|---|---|---|---|---|---|---|---|
| f1 | — | 5.540e-6 | — | f13 | 1.680e-3 | 2.048e-3 | +21.9% |
| f2 | — | 1.075e-5 | — | f14 | 2.244e-3 | 2.494e-3 | +11.2% |
| f3 | — | 3.118e-5 | — | f15 | 2.920e-3 | 3.011e-3 | +3.1% |
| f4 | — | 7.775e-5 | — | **f16** | 3.720e-3 | 3.621e-3 | **−2.7% ←交叉** |
| f5 | 4.803e-6 | 1.546e-4 | ×32.2 | f17 | 4.639e-3 | 4.319e-3 | −6.9% |
| f6 | 2.539e-5 | 2.668e-4 | ×10.5 | f18 | 5.667e-3 | 5.101e-3 | −10.0% |
| f7 | 7.581e-5 | 4.165e-4 | ×5.5 | f19 | 6.785e-3 | 5.964e-3 | −12.1% |
| f8 | 1.723e-4 | 5.961e-4 | ×3.5 | f20 | 7.987e-3 | 6.874e-3 | −13.9% |
| f9 | 3.263e-4 | 8.108e-4 | ×2.5 | f21 | 9.271e-3 | 7.855e-3 | −15.3% |
| f10 | 5.482e-4 | 1.061e-3 | +93.5% | f22 | 1.062e-2 | 8.967e-3 | −15.6% |
| f11 | 8.435e-4 | 1.347e-3 | +59.7% | f23 | 1.203e-2 | 1.016e-2 | −15.5% |
| f12 | 1.219e-3 | 1.666e-3 | +36.7% | f24 | 1.349e-2 | 1.151e-2 | −14.7% |

B 的相对劣势从 f5 的 ×32 单调衰减,f16 翻号,之后稳定在 −15% 平台。

### A.3 分段 / 聚合 / 端点

| 指标 | A | B | Δ | 判读 |
|---|---|---|---|---|
| seg-MSE short(f5-10) | **1.921e-4** | 5.509e-4 | +186.7% | A 2.9× |
| seg-MSE mid(f11-17) | **2.466e-3** | 2.644e-3 | +7.2% | A 胜 |
| seg-MSE long(f18-24) | 9.407e-3 | **8.061e-3** | −14.3% | B 胜 |
| GM-MSE(f5-24) | **1.324e-3** | 2.216e-3 | +67.4% | A 1.67× |
| 对齐 4 帧算术均值(旧口径) | 2.865e-3 | **2.775e-3** | −3.1% | 打平(晚帧主导) |
| MSE@f24(端点) | 1.349e-2 | **1.151e-2** | −14.7% | B 胜 |
| FDE(末帧逐点 L2) | 1.560e-1 | **1.489e-1** | −4.5% | 近平 → A 长尾 |
| Chamfer(全程均值) | **5.182e-2** | 5.452e-2 | +5.2% | A 胜 |

### A.4 Procrustes 位姿/形状分解(全锚帧)

| 帧 | 质心偏移 A / B | 旋转 A / B | 尺度 s A / B | 残余形状 MSE A / B(B/A) |
|---|---|---|---|---|
| f5 | 6.04e-4 / 4.39e-3 | 0.34° / 1.27° | 1.0007 / 1.0128 | **2.44e-6** / 1.08e-4(44×) |
| f10 | 6.50e-3 / 1.14e-2 | 5.23° / 5.15° | 1.0016 / 1.0249 | **1.51e-4** / 5.95e-4(3.9×) |
| f15 | 1.77e-2 / 1.91e-2 | 14.64° / 13.27° | 1.0000 / 1.0381 | **3.33e-4** / 1.06e-3(3.2×) |
| f20 | 3.28e-2 / 2.83e-2 | 25.16° / 22.73° | 0.9861 / 1.0343 | **8.48e-4** / 2.22e-3(2.6×) |
| f24 | 5.09e-2 / 4.86e-2 | 33.46° / 34.36° | 0.9691 / 1.0244 | **1.50e-3** / 3.14e-3(2.1×) |

派生·可对齐位姿分量(单帧 MSE − 残余):f15 A 2.59e-3 / B **1.96e-3**;f20 A 7.14e-3 / B **4.65e-3**;f24 A 1.20e-2 / B **8.36e-3**。
解读:位姿漂移幅度晚帧两臂相当,但位姿对 MSE 的贡献 A 更大(长尾);形状轴 A 全程 2~4× 胜;s 方向相反(A 偏大 3.1%,B 的 RMS 半径偏小 2.4%)。

### A.5 固定边弥散度(k=8 首帧 kNN 边长膨胀率;⚠️ 仅诊断)

| 帧 | A:pred / GT / 超额 | B:pred / GT / 超额 |
|---|---|---|
| f5 | +0.59 / +0.60 / **−0.01%** | −0.65 / +0.60 / −1.25% |
| f10 | +1.32 / +1.31 / **+0.01%** | −1.24 / +1.31 / −2.56% |
| f15 | +1.98 / +1.65 / +0.33% | −2.58 / +1.65 / −4.23% |
| f20 | +3.45 / +1.29 / +2.16% | −3.47 / +1.29 / −4.76% |
| f24 | +5.43 / +1.17 / **+4.26%** | −3.15 / +1.17 / **−4.33%** |

A 膨胀、B 收缩,幅度对称;结合 A.4 的 s 与 A.6 的凸包 → A = 均匀膨胀,B = 核心收缩 + 外壳外溢。

### A.6 体积 / 物理合理性

| 指标 | A | B | 判读 |
|---|---|---|---|
| vMSE(帧差速度) | 9.530e-5 | **7.612e-5** | B −20.1% |
| aMSE(二阶差) | **6.466e-6** | 8.978e-6 | B +38.8% |
| 体积相对误差 vs GT | **4.79%** | 14.15% | A 3× 好 |
| 体积自漂移 pred(GT 基线 10.89%) | 9.98% | 9.26% | 聚合均近 GT |
| 超额自漂移(聚合) | −0.91% | −1.63% | 均近零(早负晚正抵消) |
| 体积超额 f10 / f15 | −3.62% / −4.01% | −9.71% / −4.89% | B 早段更欠形变 |
| 体积超额 f20 / f24 | **+1.86% / +4.28%** | +4.83% / +9.97% | B 晚帧外溢 2.3× |
| 穿透率 / 深度 | 0.424% / 8.86e-4 | **0.000% / 0** | B 完美(唯一物理赢项) |
| loss_F pred / GT-ref | —(output=1 跳过) | 3.152e-1 / 6.168e-2(**5.11×**) | B 形变一致性劣于 GT |

### A.7 成本(同卡 5090、90000 步、有效 batch 8)

| | A | B | 比值 |
|---|---|---|---|
| 训练 wall-clock | **25h24m**(1.02 s/it) | 44h03m(1.76 s/it) | 1.73×(实测) |
| 推理 | 20 前向 × 6 帧注意力(eval 实测 4.06 s/窗,含指标计算) | 50 步 DDIM × 26 帧注意力 | ~10×(估算,待实测) |

全表 caveat:n=13、B 单 seed(DDIM seed=0)、条件信息不对等(1 vs 5 GT 帧)、范式捆绑对比、Umeyama 对非刚体是投影(「旋转/尺度」部分吸收形变)。
