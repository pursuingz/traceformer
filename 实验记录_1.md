# Traceformer 一期实验记录

> 本文件是“一期：通用多材质物理仿真”的当前实验主台账。
> 研究目标与验收标准以 `研究目标与路线图.md` 为准；旧实验和已关闭路线见 `实验记录.md`。

## 1. 一期验收目标

| 编号 | 目标 | 核心证据 |
|---|---|---|
| A | 长程 rollout 精度可用 | full/long MSE、FDE、逐帧误差曲线，且 short horizon 不明显退化 |
| B | 多材质表现均衡 | elastic / plasticine / sand 分别报告；elastic 出色，plasticine 和 sand 稳定 |
| C | `E/nu` 明显影响运动 | counterfactual、sensitivity、参数交换及分参数区间响应 |
| D | 物理合理性不退化 | 质心/形状分解、体积、速度/加速度、穿透及视觉失败案例 |

## 2. 固定实验口径

- 当前阶段：一期。
- 每个实验必须注明服务于 A/B/C/D 中的哪一项目标。
- 主要结果必须使用冻结 test split，并分别报告 overall、elastic、plasticine、sand。
- 主要长程指标：`full_rollout_mse`、`long_mse`、`FDE` 和 per-frame error curve。
- 诊断指标：GM-MSE、f24 centroid error、f24 shape residual MSE、体积、速度/加速度和地面穿透。
- 输入/输出帧定义不同的模型不得直接比较含条件帧的聚合标量；使用对齐的 prediction horizon 指标。
- checkpoint knockout 是同一已训练模型内的因果诊断，不是独立训练 baseline。
- 定量结论必须挂对应代码 commit、config、checkpoint、seed、样本数和结果文件。

## 3. 当前基线与活跃路线

### 3.1 当前比较基线

- **当前一期主基线：** `mm3_contact_cond@90k`。
- **训练 config：** `src/configs/config_mm3_contact_cond.yaml`。
- **评测 config：** `src/configs/eval_mm3_contact_cond.yaml`。
- **checkpoint：** `outputs/mm3_contact_cond_8L/checkpoint-90000/model.safetensors`。
- **测试口径：** 冻结 41-model held-out test，elastic / plasticine / sand = 13 / 14 / 14；主结果按材质分别报告。
- **代码身份：** contact-cond 实现主提交为 `5635ad6`；B2 诊断实现为 `66b35d7`。启动 B3 前还必须把服务器实际训练 checkpoint 的 provenance 与 config 再核对一次。
- v11a/HST 只作为已完成机制实验，不再作为当前方法基线；其冻结结果见 `实验记录.md`。

### 3.2 活跃研究轴

1. **当前主轴：** `E/nu/material` 与粒子历史状态的联合表示和交互式注入。
2. 在保留 8 层串行主干和 contact-cond 基线的前提下，研究材料条件在 block 内的注入位置、频率和调制对象。
3. 检查材料响应增强是否同时改善或保持长程精度，而不是只制造更大的 counterfactual 输出差异。
4. Transformer 内部状态反馈和跨层信息流只在出现新机制证据时重开，不继续沿用已关闭的 v11a/simple dynamic-gate 方案。

### 3.3 暂缓方向

- 任意形状边界和复杂障碍物：二期再启动。
- 已被历史实验关闭的架构、rollout-training 和输出参数化变体：除非出现新的机制证据，不重复投入完整训练预算。
- B1b 原预注册 dynamic-gate 路线：当前判定 `close`，不能直接推进原方案。

## 4. 当前实验总览

| 实验 | 服务目标 | 核心问题 | 状态 | 代码 commit | 结论 |
|---|---|---|---|---|---|
| B0 材料条件依赖诊断 | C | 模型是否实际使用材料条件 | 已完成诊断 | `a7b32f3`–`d813fa1` | 依赖具有明显类别差异；只测依赖，不证明反事实正确 |
| B0.1 `E/nu/class` 分解诊断 | C | 连续参数和类别 token 分别承担什么作用 | 已完成诊断 | `a12feea`–`b6b9204` | `mat_type` 主导；`E` 选择性有效；`nu` 接近 ignored/ambiguous |
| B0.2 Material Identifiability Audit | B / C / D | 数据本身是否可区分材料参数，及材料参数是否被静态因素混杂 | 已完成，数据条件通过 | `f6096c9`、`140e279`、`7aa1fe1`、`8be7c7f`、`0ab27d0`、`e9adaf2`、`bb02f9d`、`b2ee92a`、`4847674`、`d7e3ec2`、`00bd5ab`、`7a783a7`、`742c092` | train 内无严重静态混杂；`E/nu` 对 GT 体积应变具有稳定独立信号，`E` 的跨响应作用强于 `nu`；模型材料敏感性不足不能归因于数据完全无信号 |
| B0.3 Prediction-GT Material Response Fidelity Audit | B / C / D | 冻结 baseline 是否复现 B0.2 已确认的、由位置轨迹可观测的材料响应 | 实现完成，等待服务器运行 | `5c26285`、`53a1d19`、`3ec0e07`、`8a2683a`、`92d8446`、`7227e22`、`0ea49cd`、`b33ba5c`、`dcaea99`、`0fc167b`、`61f9c0f` | 不预写结论；服务器运行后才以 factual prediction-GT paired statistics 判断模型响应保真度 |
| B1a v11a MC-HST | A/B/C | 全局状态与材料调制反馈能否改善长程预测 | 已完成，混合结果 | `8fb0cd2`–`06b636b` | 无 contact 时部分指标改善，但不能解决接触和 plasticine 晚程问题 |
| B1b HST feedback diagnostic | A/B/C | 四个 feedback stage 是否具有材料相关作用 | 已完成诊断 | `b1de684` | stage feedback 的能量与误差关系需结合 gate knockout 判断 |
| B1b HST gate knockout | A/B | 关闭不同 trained gate 是否呈现 plasticine/sand 相反响应 | 已完成，预注册 `close` | `b6453db`–`b551b5c` | stage0 材料冲突明显，但 overall 安全阈值失败；stage1 疑似有害；stage2 对 elastic/sand 承重 |
| B2 `E/nu` 连续响应扫描 | C/D | 冻结模型是否对连续材料参数产生可见且方向合理的响应 | 已完成，判定 `revise` | `66b35d7` | `E` 在 sand 上有明显响应；`nu` 整体很弱，当前显式材料条件尚未达到目标 C |
| B3a Joint Material-State Adapter | A/B/C/D | 联合状态与材料条件能否提高材料控制，同时保持三材质长程精度 | 已关闭 | `9ecfa0a`、`442491e`、`b8bf90b`–`aec2044` | overall/sand 改善但 elastic/plasticine 与穿透退化；B3b 未能修复幅度冲突，关闭 B3 adapter 家族 |
| B3b Material-Stage Gate Screening | A/B/C | 仅调节 B3a 共享 adapter 的每材质、每 stage 更新幅度能否修复材料失衡 | 已完成，预注册 `close` | `0b08a6a`–`c77e5f7` | 所有学习 gate 仍接近 identity（0.9945–1.0013），dev-test 与 B3a 几乎重叠，否定仅修复幅度的假设 |
| B4 Continuous Material AdaLN | A/B/C/D | 直接以连续 `E/nu` 调制每层 AdaLN 能否提高条件控制权且保持 rollout 安全 | 已完成，预注册 `close` | `436b564`–`92fafbb` | 路径明显承重且 sand 改善，但 plasticine long/FDE 分别恶化 294%/110%，穿透增加 2.022 pp；Gate A 失败，未运行 Gate B sweep |

## 5. B0.2 Material Identifiability Audit

- **日期：** 2026-08-08。
- **服务目标：** B / C / D。
- **研究问题：** `mm3` 数据中，`log10(E)` 与 `nu` 是否在每种材料内具有可分辨的 GT 动态响应；这些参数是否可被初始几何、重力、拖拽或初始接触等静态因素预测；`mm3_test` 是否落在 `mm3_train` 的参数与静态特征支持域内。
- **数据：** `mm3_train` 使用完整响应；`mm3_test` 仅做静态 coverage。统计单位是 object（一个 H5/model），不是帧或粒子。
- **训练 / checkpoint：** 无 / 无。本审计不加载模型，不导入 CUDA，也不运行 rollout。
- **测试隔离：** 严禁读取 test GT dynamics；test 仅读取 `x[0]` 和静态元数据，`v/F/C` 与未来轨迹不得用于统计、阈值选择或裁决。
- **混杂口径：** future contact 是 response，不是 nuisance；support 联合空间包含 `log10(E)`、`nu` 与初始静态特征，混杂/响应模型的 nuisance 仅为初始静态几何、重力、拖拽和初始接触特征。
- **冻结统计设置：** `seed=0`、`folds=5`、`permutations=500`、`bootstrap_samples=1000`、`contact_band_raw=0.08`；对象名称排序后再按 seed 置换，所有拟合、标准化、缺失值处理和内层选参均只从训练 fold 得到。
- **冻结阈值：** 参数生成支持域为 `log10(E)=[4.0, 7.0]`、`nu=[0.05, 0.45]`，coverage 使用 5×5 固定网格；任一参数 test 超出 train 范围比例 `>0.05`、train-empty joint bin 比例 `>0.20`、Mahalanobis 超阈比例 `>0.20` 或 test 静态特征非有限即为 `out_of_support`。混杂判定为 nuisance 预测参数的 object-level CV `R² > 0.05` 且 permutation `p < 0.05`。
- **冻结响应判定：** 每个 `material × parameter` 的 primary response 只有在 conditional `delta_R² >= 0.05`、permutation `p < 0.05`、FDR `q < 0.05`、bootstrap CI 下界 `>0` 且未混杂时才可判为 `identifiable`；secondary response 不得单独触发该结论。任一响应 conditional `delta_R² >= 0.01` 或具有部分显著性仅为 `weak`。`constant_response` 是合法的“无证据”行，不触发统计损坏；只有 missing、fit failure 或本应有限却非有限的统计才触发 `invalid`。support shift 单独报告，不与混杂混为同一裁决。
- **协议修订（2026-08-09，方案 A）：** `E` 使用 `Mnu -> Mboth`，`nu` 使用 `ME -> Mboth`；partial Spearman 从参数和响应两侧 cross-fit 去除静态 nuisance 与另一参数，conditional residual permutation 以静态 nuisance + 另一参数预测目标参数后置换残差并重建，bootstrap 比较相同 conditional reference 与 `Mboth`。保留固定 object folds 和 train-fold-only preprocessing。该量是 observational conditional incremental association，不是 counterfactual causality。
- **完整性修订（2026-08-09）：** 同时为空的 generator `drag_force/drag_mask` `(0,)` 规范化后再校验，非空一维数组仍拒绝；读取前拒绝 train/test 同目录、同 H5 或 object/model ID 重叠，并要求 train/test 各自覆盖三种材质。train invalid 与 test invalid 分轴记录：前者可使 train `status` 不可裁决，后者只使 support 为 `unknown`。metadata 固定写入 `schema_version=1.0`、UTC ISO-8601 `generated_at` 与 `audit_integrity_status`；任一七文件目标已存在时在昂贵统计前失败，writer 激活前再次复查。
- **Residual re-review 修复（2026-08-10）：** test invalid 的 support override 统一在输出准备入口传播到 `summary.csv`、`coverage.csv` 与 Markdown `Train/Test Support` 表；三者均输出 `unknown`，不保留 stale `test_parameter_in_support`，且不改写 train `status`。exact seven outputs、公开 API 与原子写入保持不变。
- **Legacy elastic schema 兼容（2026-08-10）：** 服务器 254 个 elastic H5 的 `F/C` 为 `(T,N,9)`，而新 plasticine/sand 数据为 `(T,N,3,3)`。审计器现与训练物理路径一致，在内存中按 row-major 将合法的 9 维矩阵展开格式规范化为 `3×3`；其他 shape 继续拒绝。材料有效计数失败时同时输出 split-level invalid reason 计数，避免只报告 `0 valid records`。
- **数据生成事实：** `np.random.randint(0,1)` 记录为现有协议事实（plasticine/sand 恒为 0），本 B0.2 不在此处修复或重写数据协议。
- **后续约束：** B0.2 已完成裁决。新训练前先用冻结 baseline 检查模型是否复现本审计确认的 GT 材料响应；在此之前不通过继续扩大 condition encoder 代替机制定位。
- **代码 commit：** `f6096c9`、`140e279`、`7aa1fe1`、`8be7c7f`、`0ab27d0`、`e9adaf2`、`bb02f9d`、`b2ee92a`、`4847674`、`d7e3ec2`、`00bd5ab`、`7a783a7`、`742c092`。
- **固定输出：** `material_identifiability_records.csv`、`material_identifiability_coverage.csv`、`material_identifiability_confounding.csv`、`material_identifiability_response.csv`、`material_identifiability_summary.csv`、`material_identifiability_metadata.json`、`material_identifiability_b02.md`。

服务器命令（从 `src/` 运行）：

```bash
python diagnose_material_identifiability.py \
  --train-dir mm3_data/mm3_train \
  --test-dir mm3_data/mm3_test \
  --output-dir results/material_identifiability_b02 \
  --seed 0 \
  --folds 5 \
  --permutations 500 \
  --bootstrap-samples 1000 \
  --contact-band-raw 0.08
```

### 5.1 完整性与覆盖结果

- **实际运行时间 / 协议：** 2026-08-10 至 2026-08-11；`seed=0`、5-fold nested CV、500 次 permutation、1000 次 bootstrap。
- **有效数据：** train `782/782`，test `41/41`；invalid record 为 0。Train 分材质为 elastic/plasticine/sand=`254/263/265`，test=`13/14/14`。
- **参数覆盖：** 三种材质的 train `log10(E)` 约覆盖 `[4,7]`，`nu` 约覆盖 `[0.05,0.45]`；每个对象的参数组合唯一。Train 内 `E/nu` 的 Pearson/Spearman 相关均接近 0，固定 5×5 joint-grid occupancy 均为 1.0。
- **静态混杂：** nuisance 对参数的 object-level CV `R²` 为 elastic `E=0.00024`、elastic `nu=0.02235`、plasticine `E=-0.02563`、plasticine `nu=-0.02644`、sand `E=-0.02424`、sand `nu=-0.02252`，全部低于冻结的 `0.05` 混杂阈值。Elastic `nu` 虽有 permutation `p=0.00998`，但解释率只有 2.2%，按预注册不判为严重混杂。
- **Test support：** plasticine 和 sand 判为 `in_support`。Elastic 的严格 range 规则将两个不同样本的极小边界越界记为 `out_of_support`：最低 `log10(E)` 只比 train 最小值低约 `8.39e-4`，最低 `nu` 只低约 `2.84e-5`。因此该标签解释为“总体在支持域内、存在轻微边界外样本”，不能表述为 elastic test 整体明显 OOD。

### 5.2 分材质 GT 响应

以下为冻结的 primary response 中最有实质意义的 conditional incremental association；`delta_R²` 衡量在控制静态 nuisance 和另一材料参数后，加入当前参数带来的交叉验证解释率增量。

| material | parameter | response | delta_R² | partial Spearman | FDR q | 裁决 |
|---|---|---|---:|---:|---:|---|
| elastic | `log10(E)` | `volumetric_strain_f24` | 0.5437 | -0.5620 | 0.0313 | 强且稳定 |
| elastic | `nu` | `volumetric_strain_f24` | 0.3001 | -0.3884 | 0.0469 | 强且稳定 |
| plasticine | `log10(E)` | `volumetric_strain_f24` | 0.8659 | -0.8416 | 0.00494 | 极强 |
| plasticine | `log10(E)` | `f_strain_norm_f24` | 0.05525 | -0.3484 | 0.00494 | 中等 |
| plasticine | `log10(E)` | `velocity_rms_trajectory` | 0.00567 | -0.4937 | 0.0171 | 方向稳定但解释率增量小 |
| plasticine | `nu` | `volumetric_strain_f24` | 0.1558 | -0.4622 | 0.0117 | 明确 |
| sand | `log10(E)` | `volumetric_strain_f24` | 0.7443 | -0.7725 | 0.00303 | 极强 |
| sand | `log10(E)` | `centered_shape_mse_f24` | 0.1345 | -0.4484 | 0.00303 | 强 |
| sand | `log10(E)` | `f_strain_norm_f24` | 0.05152 | -0.2559 | 0.00303 | 中等 |
| sand | `log10(E)` | `velocity_rms_trajectory` | 0.01169 | -0.4803 | 0.00303 | 明确 |
| sand | `nu` | `volumetric_strain_f24` | 0.1535 | -0.5305 | 0.0104 | 明确 |
| sand | `nu` | `f_strain_norm_f24` | 0.03333 | +0.1192 | 0.0104 | 弱到中等 |

- `E` 的可辨识作用总体强于 `nu`，尤其在 plasticine/sand 上同时关联速度、应变、体积和形状。
- `nu` 的可靠信号主要集中在体积应变及部分 F-strain；不能据此宣称 `nu` 普遍控制整体速度或质心运动。
- Elastic 的 primary 信号集中在体积应变；其 velocity、centroid、centered-shape 和 `f_strain_norm_f24` 未得到稳定独立增益。
- Plasticine 的 centroid displacement 虽有很小的显著 `delta_R²`，增量只有 `1e-4` 量级，不作为实质材料控制证据。

### 5.3 科学裁决

- **数据条件通过。** 现有 mm3 train 在三种材质内均存在至少一个强、未被静态 nuisance 解释的连续材料响应；最稳定的共同响应是 `volumetric_strain_f24`。
- **不能把模型问题归因于“数据没有 E/nu 信号”。** B0/B0.1/B2 中模型对 `nu` 很弱，而 GT audit 显示 `nu` 对体积应变存在明确独立关联；差距更可能位于模型读取、表示、目标权重或训练优化路径。
- **自动的 `identifiable` 总标签需要限定。** 它表示“至少一个冻结 primary response 通过”，不表示该参数会明显控制所有运动量，也不证明 counterfactual 因果正确。
- **跨材质比较仍受生成协议混杂。** Elastic 与 plasticine/sand 的 drag/floor 场景不同；以上裁决只使用材质内连续参数变化，不把跨材质差异当作 `E/nu` 证据。
- **下一步：** 在不训练新模型的前提下，对冻结 `mm3_contact_cond@90k` 提取 prediction 与 GT 共有的位置响应并比较 response fidelity；优先检查速度/加速度 RMS、质心位移、去刚体平移后的形状 MSE、几何凸包体积变化和接触比例。当前模型不输出 `F/C`，因此 B0.3 不伪造 `volumetric_strain_f24` 或 `f_strain_norm_f24`。只有定位模型在哪一共享位置响应上丢失材料作用后，才注册下一种条件注入结构。

### 5.4 B0.3 Prediction-GT Material Response Fidelity Audit

- **日期：** 2026-08-11（实现与高强度验证）；真实服务器推理尚未运行。
- **服务目标：** B / C / D。
- **研究问题：** 对冻结的 `mm3_contact_cond@90k`，模型在事实条件（每个 test object 自己的 `E`、`nu`、`mat_type` 和其他物理条件不改动）下预测的材料响应，是否与同一对象的 GT 位置轨迹响应保持方向和量级一致。该审计承接 B0.2：B0.2 已证明数据存在 GT 信号；B0.3 检查模型是否复现该信号。
- **无训练边界：** 不训练、不微调、不做 checkpoint 选择、不做条件交换或 counterfactual generation；不修改 `eval.py`、模型、训练路径、训练配置或 checkpoint。全部结论仅来自已冻结 checkpoint 的 factual rollout。
- **冻结基线与口径：** `mm3_contact_cond@90k`，`configs/eval_mm3_contact_cond.yaml`，`outputs/mm3_contact_cond_8L/checkpoint-90000/model.safetensors`；冻结 `mm3_test`，41 个 `start_idx=0` object，elastic / plasticine / sand=`13 / 14 / 14`，每个 object 恰好一次 rollout，`seed=0`。
- **shared position observables：** prediction 与 GT 均使用同一 `(25, N, 3)` 归一化位置坐标、同一帧定义和同一函数计算：`position_velocity_rms_trajectory`、`position_acceleration_rms_trajectory`、`centroid_displacement_f24`、`centered_shape_mse_f24`、`hull_volume_relative_change_f24`、`hull_volume_relative_change_trajectory`；secondary observables 为 `extent_change_x/y/z_f24` 与 `future_contact_fraction`。凸包仅是点云几何体积，禁止表述为 `det(F)` 或变形梯度。
- **统计协议：** factual fidelity 报告 GT/pred 均值与标准差、MAE、RMSE、bias、Spearman(pred, GT) 和 object-level paired bootstrap CI；参数对齐同时报告 ordinary 与仅控制另一连续参数的 partial Spearman。bootstrap 的重采样单位始终是 object，不是帧、粒子或长表行；常数 response 合法并以空相关/CI 记录。标签优先级冻结为 `reversed > attenuated > aligned > weak_or_unresolved`。
- **固定六输出：** `material_response_fidelity_b03_models.csv`、`material_response_fidelity_b03_responses.csv`、`material_response_fidelity_b03_fidelity.csv`、`material_response_fidelity_b03_alignment.csv`、`material_response_fidelity_b03_metadata.json`、`material_response_fidelity_b03.md`。默认拒绝覆盖；只有确需替换完整旧输出时才传 `--overwrite`，写入过程具有暂存、备份和失败回滚保护。
- **设计 / 实现 commit：** 设计 `5c26285`，实施计划 `53a1d19`，标签优先级 `3ec0e07`，位置响应 `8a2683a`，统计 `92d8446` / `7227e22`，六输出与 provenance `0ea49cd` / `b33ba5c`，事实 rollout runner 与审查修复 `dcaea99` / `0fc167b` / `61f9c0f`。
- **高强度验证：** 相关测试模块全量、`py_compile`、`git diff --check` 和独立代码审查均在本任务完成前执行并记录到 `.superpowers/sdd/2026-08-11-b03-material-response-fidelity-implementation/task-5-report.md`。真实 CUDA/H5 推理不作为本地验证的一部分，也尚未运行。
- **服务器命令（从 `src/` 运行）：**

```bash
python diagnose_material_response_fidelity.py \
  --config configs/eval_mm3_contact_cond.yaml \
  --checkpoint outputs/mm3_contact_cond_8L/checkpoint-90000/model.safetensors \
  --output-dir results/material_response_fidelity_b03 \
  --seed 0 \
  --bootstrap-samples 10000 \
  --contact-band-raw 0.08
```

- **状态：** **实现完成，等待服务器运行。** 不预写模型优劣、材料方向、置信区间或任何虚构数值；结果写入后再独立登记分析与裁决。

## 6. B1b HST Gate Knockout

- **目的：** 判断 HST 各 stage 的静态 feedback gate 是否对不同材质产生相反作用，并决定是否推进原预注册 dynamic gate。
- **样本：** 冻结的 41-model、`start_idx=0` full-horizon test；elastic 13、plasticine 14、sand 14。
- **条件：** `normal`、`all_off`、`stage0_off`、`stage1_off`、`stage2_off`；同一 checkpoint、输入和 seed 配对。
- **规模：** raw 205 行，paired 164 行，bootstrap 10000，seed 0。
- **checkpoint：** `outputs/mm3_v11a_contact_cond_8L/checkpoint-90000/model.safetensors`。
- **config：** `configs/eval_mm3_v11a_contact_cond_8L_45k.yaml`。
- **代码 commit：** `b6453db`、`6f06296`、`a711fd3`、`34ee289`、`4367caa`、`203cd7b`、`2dade53`、`b551b5c`。
- **结果文件：** `hybrid_state_gate_knockout_b1b_90k.md`、`hybrid_state_gate_knockout_b1b_90k_raw.csv`、`hybrid_state_gate_knockout_b1b_90k_paired.csv`。

### 6.1 关键结果

| knockout | elastic full MSE | plasticine full MSE | sand full MSE | 解释 |
|---|---:|---:|---:|---|
| all_off | +1.20% | -24.72% | +66.54% | HST 明显帮助 sand，但伤害 plasticine |
| stage0_off | +5.08% | -15.03% | +52.73% | 最强材料相反响应，但 overall 不安全 |
| stage1_off | -2.61% | -10.49% | -4.40% | 三种材质方向一致改善，疑似冗余或有害反馈 |
| stage2_off | +10.35% | -1.61% | +12.75% | 对 elastic 和 sand 承重，对 plasticine 近中性 |

所有百分比均为 `knockout - normal` 相对 normal 的变化；负值表示误差改善。

### 6.2 结论

- 原预注册判定：`close`，`proceed_dynamic_gate=False`。
- stage0 的材料相反响应成立，但关闭后 overall full-rollout MSE 上升 34.48%，不能直接推进简单 dynamic gate。
- stage1 是后续最值得用独立从头训练消融验证的候选，但 checkpoint knockout 不能证明删除后重训一定更好。
- stage2 对 elastic 和 sand 的有效状态修正明确，应避免无依据裁剪。
- 关闭 HST 可显著降低穿透，却同时严重损害 sand 轨迹；穿透改善可能来自运动被抑制，不能单独作为物理改善结论。

## 7. B2 `E/nu` 连续响应扫描

- **日期：** 2026-08-04。
- **服务目标：** C / D。
- **研究问题：** 冻结的 `mm3_contact_cond@90k` 是否真正利用连续 `E/nu`，以及响应方向是否满足基本 constitutive sanity。
- **唯一变量：** 每次只改变一个推理条件：`log10(E)=4.5/5.5/6.5` 或 `nu=0.10/0.25/0.40`；另一个连续参数和 `mat_type` 固定为该模型真实值。
- **冻结项：** `mm3_contact_cond@90k` checkpoint、41 个 `start_idx=0` 模型、输入轨迹、点采样、推理协议与同模型同 condition seed；模型和 checkpoint 全程只加载一次。
- **样本：** 41 models（elastic 13、plasticine 14、sand 14）× 7 conditions = 287 paired rollouts。
- **代码 commit：** `66b35d7`。
- **评测 config：** `configs/eval_mm3_contact_cond.yaml`。
- **checkpoint：** `outputs/mm3_contact_cond_8L/checkpoint-90000/model.safetensors`。
- **seed：** 0。
- **输出：** `material_response_sweep_b2_raw.csv`、`material_response_sweep_b2_model_summary.csv`、`material_response_sweep_b2_summary.csv`、`material_response_sweep_b2.md`。
- **解释限制：** 没有 counterfactual GT，因此不能把扫描结果解释为反事实预测准确率；只判断响应强度、单调性和预注册方向。
- **完整性：** 287 raw rows、41 model summaries；elastic 13、plasticine 14、sand 14；provenance 一致。
- **当前状态：** 已完成，判定 `revise`。
- **服务器命令（从 `src/` 目录执行）：**

```bash
python diagnose_material_response_sweep.py \
  --config configs/eval_mm3_contact_cond.yaml \
  --checkpoint outputs/mm3_contact_cond_8L/checkpoint-90000/model.safetensors \
  --output-dir /root/results/traceformer/material_response_sweep_b2 \
  --seed 0
```

### 7.1 预注册汇总结果

| group | n | mean E response MSE | mean nu response MSE | E-shape weak monotonic | nu-volume weak monotonic | ignored | non-monotonic | plausible |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| overall | 41 | `1.416964e-3` | `6.207402e-5` | 34.1% | 14.6% | 87.8% | 9.8% | 2.4% |
| elastic | 13 | `2.084445e-5` | `2.494619e-5` | 15.4% | 15.4% | 100.0% | 0.0% | 0.0% |
| plasticine | 14 | `6.599834e-4` | `1.678421e-5` | 0.0% | 14.3% | 100.0% | 0.0% | 0.0% |
| sand | 14 | `3.470342e-3` | `1.418397e-4` | 85.7% | 14.3% | 64.3% | 28.6% | 7.1% |

- overall 的平均 `E` response MSE 是 `nu` 的约 22.8 倍；该比值只适用于当前扫描区间，不能解释为两个物理参数的固有重要性比值。
- 预注册 2% effect-size 阈值下，36/41 模型为 `ignored`；4/41 为 `responsive_non_monotonic`，仅 1/41 为 `directionally_plausible`，无 `unstable_excessive`。
- `E` 的主要正证据集中于 sand：12/14 模型满足 E-shape 宽松单调方向，中位 `rho=-1.0`。
- `nu` 的预注册 volume 方向仅 6/41 模型满足，三种材质均没有稳定证据。

### 7.2 事后诊断（不替代预注册判定）

- 扫描值与真实值距离越大，`E` response 通常越大：overall Spearman `rho=0.530`；elastic / plasticine / sand 分别为 `0.631 / 0.771 / 0.705`。说明 `E` token 并非完全失效，而是 causal authority 高度依赖材质。
- `nu` 的 distance-response 仅 overall `rho=0.327`；elastic 为 `0.804`，但绝对响应很小；plasticine / sand 仅 `0.068 / 0.131`，缺乏平滑连续响应。
- plasticine 的平均 `E` 响应主要由 `E=10^4.5` 条件贡献；不能仅凭三点均值声称其已学到完整单调本构关系。
- sand 的 `E` 响应存在明显长尾；5/14 模型超过 2% 阈值，最高模型 response ratio 为 23.2%。overall 均值不能代表所有 sand 样本。

### 7.3 指标与数据限制

- `ignored` 表示响应未超过 normal coordinate-motion energy 的 2%，不是数学意义的零响应；该阈值应和 raw response curve 一起解读。
- sand 的凸包体积相对变化非常不稳定，normal 组均值约 434%，个别模型超过 3000%；因此 `nu-volume` 只能作为粗糙 sanity check，不能单独判定 `nu` 的物理正确性。
- 固定五帧历史本身携带隐式材料动态；反事实修改 `E/nu` 会使显式 token 与历史状态产生冲突。弱响应说明显式条件在冲突下权威不足，但不等同于网络完全无法从历史推断材料。
- 当前数据生成把材质类别与激励方式耦合：elastic 使用拖拽且无重力，plasticine/sand 的 `np.random.randint(0, 1)` 恒为 0，因此总是重力落地。类别、重力、接触历史可替代连续参数，削弱 `E/nu` token 的必要性。
- 无 counterfactual GT，不能判断某条扫描轨迹是否准确；若要验证可控性与物理正确性，仍需少量同几何、同外力的配对参数网格 GT。

### 7.4 结论

- **目标 C 当前未达成。** 模型不是完全忽略材料参数，但显式连续条件的作用不普遍、不均衡，尤其 `nu` 明显不足。
- `E` 在 sand 上的强响应和单调 shape 趋势是可保留的正证据；elastic/plasticine 的 2% 阈值结果不足以支持显式条件承重。
- 判定为 `revise`：不关闭材料条件路线，但下一步必须提高 `E/nu/material` 联合表示的 causal authority，并把历史状态与显式材料参数之间的冲突作为设计对象。

## 8. B3a Factorized Material-State Adapter

- **日期：** 2026-08-04。
- **服务目标：** A / B / C / D。
- **机制假设：** 仅把材料条件作为独立 token 使用时，其作用容易被五帧历史状态替代；在低秩空间显式计算粒子 hidden 与 `[log10(E), nu, mat_type]` 的乘性交互，可提高材料条件对状态更新的控制力。
- **唯一变量：** 在 `mm3_contact_cond` 的 8 层串行主干第 2/4/6/8 层后复用同一个 rank-64 adapter；contact path、旧 `E/nu/class` token、数据、损失、优化器和训练 schedule 全部冻结。
- **参数预算：** 新增 34,052 参数，约占基线 0.212%；8 个原始 `SpatialTemporalTransformerBlock` 不变。
- **初始化：** adapter 输出投影为零初始化；同 seed 下 baseline/B3 公共参数逐位相同，step 0 完整模型输出逐位相同。
- **训练 config：** `src/configs/config_mm3_b3a_material_state_adapter.yaml`。
- **实际 checkpoint：** `outputs/mm3_b3a_material_state_adapter_8L/checkpoint-90000/model.safetensors`。预注册要求先在 45k 执行 screening gate，但本次实际直接训练到了 90k；这是协议偏离，后续不得把 90k 结果写成 45k screening。
- **正式 90k 评测 config：** `src/configs/eval_mm3_b3a_material_state_adapter_90k.yaml`。
- **同 checkpoint knockout：** `src/configs/eval_mm3_b3a_material_state_adapter_90k_off.yaml`，仅把 `material_state_runtime_scale` 改为 0；只用于判断训练后模型是否使用 adapter，不作为独立训练 baseline。
- **评测口径：** 冻结 41-model test，elastic / plasticine / sand = 13 / 14 / 14，seed 0，5 帧输入、单帧预测、20 帧 autoregressive rollout。
- **材料响应诊断：** `diagnose_material_response_sweep.py --profile b3a90`，沿用 B2 的 41 models × 7 conditions 协议；尚未执行。
- **adapter 诊断：** `diagnose_material_state_adapter.py`，只读取 checkpoint 参数，不执行 rollout。
- **代码 commit：** B3a 实现 `9ecfa0a`；90k 诊断 profile / eval config `442491e`。
- **当前验证：** adapter/集成/config/参数/旧 contact/v11a/材料诊断相关测试通过；Python 语法检查通过；GPU 训练和标准 eval 已完成。
- **成功标准：** 45k 时 accuracy gate 与 material-response gate 同时通过；不能只放大 counterfactual 响应而损害三材质 long MSE/FDE。
- **停止条件：** 45k 任一主材质长程精度明显恶化，或 adapter-on/off 表明新增分支没有实际贡献时，不继续到 90k。

### 8.1 独立 baseline 对比（两臂均 @90k）

| 指标 | `mm3_contact_cond` | B3a ON | 相对变化 |
|---|---:|---:|---:|
| first-chunk MSE | 2.621e-5 | 2.202314e-5 | -16.0% |
| full-rollout MSE | 3.501029e-3 | 2.518895e-3 | **-28.1%** |
| long MSE | 8.910628e-3 | 6.383507e-3 | **-28.4%** |
| GM-MSE | 1.406e-3 | 1.091716e-3 | **-22.4%** |
| FDE | 1.008436e-1 | 9.877093e-2 | -2.1% |
| vMSE / aMSE | 1.073e-4 / 5.389e-5 | 8.614259e-5 / 4.382244e-5 | -19.7% / -18.7% |
| 体积相对误差 | 13.553% | 11.006% | -18.8% |
| 地面穿透率 / 深度 | 3.340% / 2.859e-4 | 5.342% / 6.888857e-4 | **+59.9% / +141.0%** |

### 8.2 分材质长程结果（B3a ON 相对独立 baseline）

| 材质 | long MSE | 变化 | FDE | 变化 | MSE@f24 变化 | 判定 |
|---|---:|---:|---:|---:|---:|---|
| elastic | 7.888317e-3 | **+13.7%** | 1.457939e-1 | +3.0% | +5.5% | long 超过 10% 退化线 |
| plasticine | 8.567929e-4 | **+58.7%** | 5.372218e-2 | **+37.7%** | **+77.6%** | 明确长程失败 |
| sand | 1.051290e-2 | **-45.0%** | 1.001555e-1 | **-19.8%** | **-44.7%** | 强改善 |

整体提升主要由 sand 驱动。Plasticine 的 GM-MSE 仅比 baseline 退化约 3.4%，但 long/FDE/f24 随 horizon 系统性恶化，说明不是单步拟合崩溃，而是 adapter 修正在 autoregressive rollout 中累积为长期失稳。

### 8.3 同 checkpoint adapter-off 机制诊断

| 指标 | B3a ON | B3a OFF | ON 相对 OFF |
|---|---:|---:|---:|
| full-rollout MSE | 2.518895e-3 | 5.066424e-3 | -50.3% |
| long MSE | 6.383507e-3 | 1.248479e-2 | -48.9% |
| GM-MSE | 1.091716e-3 | 1.974036e-3 | -44.7% |
| FDE | 9.877093e-2 | 1.178972e-1 | -16.2% |
| sand long MSE | 1.051290e-2 | 2.769769e-2 | -62.0% |
| plasticine long MSE | 8.567929e-4 | 7.419724e-4 | **+15.5%** |

OFF 比独立 baseline 也普遍更差，说明主干和 adapter 在训练中发生了明显 co-adaptation。该 knockout 证明模型实际依赖 adapter，但不能把 ON/OFF 的 50.3% 差值解释为 B3a 相对 baseline 的方法增益。Plasticine 在关闭 adapter 后长程反而改善，进一步定位到 adapter 修正本身与 plasticine 长程动力学存在冲突。

### 8.4 当前裁决

- **Accuracy gate：失败。** Elastic long MSE 退化 13.7%；plasticine long/FDE 分别退化 58.7%/37.7%；穿透率和深度产生明显新失效模式。
- **Material-response gate：待执行。** 即使材料响应增强，也不能推翻 accuracy gate，只能判断联合 material-state interaction 是否有可保留的机制价值。
- **状态：** `mixed negative / close pending mechanism diagnosis`。不再训练 B3a，不调 rank、注入间隔或学习率；先完成 `b3a90` 材料响应扫描和 adapter 参数诊断。
- **后续分叉：** 若材料响应未改善，关闭 B3 adapter 家族；若响应改善，再用低成本 stage knockout 与分材质梯度方向诊断区分“更新幅度冲突”和“更新方向冲突”，之后才决定是否提出新实验。

### 8.5 Stage knockout 与分材质梯度冲突诊断

- **日期：** 2026-08-05 至 2026-08-06。
- **服务目标：** A / B / C 的机制诊断；不训练新模型，不改变 B3a 已失败的 accuracy gate。
- **研究问题：** 四次 adapter 注入中哪些 stage 对不同材质有益或有害；elastic、plasticine、sand 对共享 adapter 的局部下降方向是幅度不同还是方向冲突。
- **冻结 checkpoint：** `outputs/mm3_b3a_material_state_adapter_8L/checkpoint-90000/model.safetensors`。
- **冻结 config：** `configs/eval_mm3_b3a_material_state_adapter_90k.yaml`。
- **stage knockout 口径：** 41 个 test model 各取 `start_idx=0`；六种条件为 `normal/all_off/stage0_off/stage1_off/stage2_off/stage3_off`；同模型同 seed；差值统一为 `knockout - normal`。`all_off` 不是独立训练 baseline。
- **梯度口径：** 41 个 model 各取 `(0,5,10,15)` 四个固定窗口，共 164 个 teacher-forced 单步样本；仅对 adapter 参数计算单步坐标 MSE 梯度，不创建 optimizer，不更新 checkpoint，不进行 full-rollout BPTT。
- **设计 commit：** `95950da`；实施计划 commit：`152a274`。
- **实现 commit：** stage mask/activity `b8bf90b`、collector 防嵌套修正 `23bfc19`、stage CLI `1b4b9b1`、梯度统计 `f90086d`、梯度 CLI `aec2044`。
- **状态：** **诊断完成，B3a 判定为 `revise`。** 允许一次有界的 material-stage gate 机制筛选；不支持直接扩展为完整分材质 expert。

服务器命令（从 `src/` 目录执行）：

```bash
python diagnose_material_state_stage_knockout.py \
  --config configs/eval_mm3_b3a_material_state_adapter_90k.yaml \
  --checkpoint outputs/mm3_b3a_material_state_adapter_8L/checkpoint-90000/model.safetensors \
  --output-dir /root/results/traceformer/material_state_stage_knockout_b3a90 \
  --bootstrap-samples 10000 \
  --bootstrap-seed 0

python diagnose_material_state_gradient_conflict.py \
  --config configs/eval_mm3_b3a_material_state_adapter_90k.yaml \
  --checkpoint outputs/mm3_b3a_material_state_adapter_8L/checkpoint-90000/model.safetensors \
  --output /root/results/traceformer/material_state_gradient_conflict_b3a90
```

实际输出：

- `material_state_stage_knockout_b3a90_raw.csv`：246 行。
- `material_state_stage_knockout_b3a90_paired.csv`：205 行。
- `material_state_stage_activity_b3a90.csv`：164 行。
- `material_state_stage_knockout_b3a90.md`。
- `material_state_gradient_conflict_b3a90.json`。
- `material_state_gradient_conflict_b3a90.md`。

#### 8.5.1 Stage knockout 结果

以下变化均为 `knockout - normal`；正值表示关闭该 stage 后误差上升，即该 stage 有益。表内为 full / long MSE 相对变化。

| knockout | elastic | plasticine | sand | 主要含义 |
|---|---:|---:|---:|---|
| `all_off` | +10.75% / +11.75% | +0.58% / **-11.42%** | **+165.21% / +161.26%** | adapter 整体对 sand 极其重要，但对 plasticine 长程有害 |
| `stage0_off` | +6.78% / +7.37% | **+22.79% / +15.83%** | **+164.48% / +164.63%** | stage0 是三材质共同承重的主要注入点，尤其支配 sand |
| `stage1_off` | -0.75% / -0.85% | **-9.04% / -9.84%** | **+13.65% / +15.68%** | plasticine 与 sand 出现稳定的相反 stage 偏好 |
| `stage2_off` | +1.69% / +1.96% | -2.87% / **-4.64%** | **+4.94% / +4.92%** | 后期注入继续帮助 sand，但损害 plasticine 长程 |
| `stage3_off` | +2.21% / +2.50% | -1.94% / -4.21% | **+4.43% / +4.64%** | 方向与 stage2 一致，效应较小 |

- `stage1_off` 在 plasticine 上改善 12/14 个 model，同时在 sand 上恶化 12/14 个 model；该相反响应不是少数离群点造成。
- `stage2_off` 的对应计数为 10/14 改善、10/14 恶化；`stage3_off` 为 9/14 改善、10/14 恶化。
- `stage0_off` 对 sand 的均值受少数大样本影响，但中位 full MSE 仍恶化约 78.95%，且 10/14 个 model 恶化，因此 stage0 的 sand 作用不是纯离群现象。
- adapter 相对 hidden 的 RMS 更新主要集中在 stage0/1：elastic 为 4.52%/1.68%，plasticine 为 14.18%/3.69%，sand 为 9.80%/3.91%。stage2/3 更新只有约 0.03%--0.05%，但仍能改变 sand 长程误差约 4%--5%，所以不能用更新幅度小直接判定 stage 无效。

#### 8.5.2 分材质梯度结果

| material | 单步坐标 MSE | adapter 梯度范数 |
|---|---:|---:|
| elastic | `5.1070e-6` | `3.8845e-6` |
| plasticine | `1.6403e-5` | `2.0848e-5` |
| sand | `4.5109e-5` | `2.0316e-4` |

- sand 的 adapter 梯度范数约为 plasticine 的 9.7 倍、elastic 的 52.3 倍；该支配程度明显大于三者单步 loss 的差异，说明共享 adapter 的首要问题是**梯度幅度失衡**。
- 全 adapter 梯度 cosine 为 elastic-plasticine `+0.135`、elastic-sand `-0.035`、plasticine-sand `+0.237`，不支持“整个 adapter 存在强烈全局方向冲突”。
- 冲突主要局部出现在 `output_proj`：elastic-sand cosine 为 `-0.163`；`state_proj` 的 elastic-sand 为 `-0.068`。当前没有 bootstrap/置信区间，不能把这些单点 cosine 过度解释为确定冲突。
- stage-scale 的 teacher-forced 单步梯度总体倾向减小 stage0 幅度，尤其是 sand；但 rollout knockout 表明 stage0 对 sand 长程最关键。说明单步目标对长程 gate 选择是近视的，不能直接用当前单步 loss 训练 stage gate 后宣称解决 long-horizon 问题。

#### 8.5.3 科学裁决

- B3a 的机制价值成立：material-state 交互确实被模型使用，并显著改善 sand；但共享、固定 stage 强度无法同时适配 plasticine 和 sand。
- 现有证据不支持立即采用完整的 per-material expert。更小且与诊断直接对应的候选是 per-material、per-stage scalar gate，同时保持共享 adapter 方向。
- 任何 gate screening 必须使用 train/val 上的 truncated rollout objective；不得在冻结 test split 上调 gate，也不得只用 teacher-forced 单步梯度优化 gate。
- 若低成本 gate screening 不能在保住 sand 的同时修复 plasticine/elastic，则关闭 B3 adapter 家族，不继续调 rank、增加 expert 或追加注入点。

## 9. 待执行实验

> 新实验只有在写清假设、唯一变量、成功标准和停止条件后才能进入完整训练。

| 优先级 | 实验 | 服务目标 | 唯一变量 | 成功标准 | 停止条件 | 状态 |
|---:|---|---|---|---|---|---|
| 1 | B4 Continuous Material AdaLN | A / B / C / D | 在 `mm3_contact_cond` 8 层串行 baseline 上新增连续 `E/nu` 到每层 AdaLN 的直接调制；保留原有 token、`mat_type` 与 contact 路径 | Gate A/B/C 全部通过：45k 精度安全、连续材料控制增强、ON/OFF 路径承重 | 任一 Gate 失败即停止 B4，不进行材料 sweep 后调参或继续 90k | **已完成，`close`** |
| 2 | B3b material-stage gate screening | A / B / C | 冻结 B3a@90k，仅训练每材质每 stage 的标量 gate；共享 adapter 和主干不变 | plasticine long 相对 B3a 改善至少 25% 且进入 baseline +10% 以内；sand 相对 B3a 恶化不超过 5%；elastic 不超过 baseline +10%；overall 与穿透不恶化 | 任一主材质越过退化线、gate 大面积饱和，或仅在 dev-test 调参后成立，则关闭 B3 adapter 家族 | **已完成，`close`** |

### 9.1 B3b Material-Stage Gate Screening

- **日期：** 2026-08-06。
- **服务目标：** A / B / C。
- **研究问题：** B3a 的共享 material-state adapter 对 sand 有明显收益，却伤害 plasticine 和部分 elastic；能否只调节每种材质、每个注入 stage 的更新幅度，在保留共享更新方向和 sand 收益的同时修复其他材质？
- **机制假设：** 主要矛盾是不同材质所需的 adapter 更新幅度不同，而不是必须拆成完整 per-material expert。采用 `3 materials x 4 stages` 的 bounded scalar gate：`g = 2 * sigmoid(theta)`，并令 `theta=0 -> g=1`，从 B3a 函数等价起点开始。
- **唯一变量：** 冻结 B3a@90k 的 8 层串行主干、contact 路径、共享 rank-64 adapter 和全部其他参数，仅训练 12 个 `gate_logits`。rigid gate 固定为 1。
- **冻结项：** 模型结构除 gate 外不变；B3a checkpoint 必须只缺少 `dit.material_state_exchange.gate_logits`，不得出现其他 missing/unexpected key。
- **设计 / 计划 commit：** `8114831` / `aa50650`。
- **实现 commit：** gate 数学 `0b08a6a`；模型集成与参数统计 `367cc5f`；calibration 数据 `ae2c64f`；冻结 rollout trainer `b96bfb5`；配置、审计修复与回归测试 `fe5f8eb`；MM3 符号链接数据兼容修复 `ed09507`；calibration 进度显示 `c77e5f7`。
- **训练 config：** `src/configs/config_mm3_b3b_material_stage_gate_screen.yaml`。
- **初始化 checkpoint：** `outputs/mm3_b3a_material_state_adapter_8L/checkpoint-90000/model.safetensors`。
- **数据隔离：** calibration 只允许读取 `mm3_data/mm3_train`；按 elastic/plasticine/sand 分层做 80/20 train/val split，seed 0，rigid 排除。当前 41-model `mm3_test` 已参与方法诊断，降级为 dev-test；gate 冻结前禁止读取。
- **起点协议：** 每个 model 构造一个 `start_idx=0` 窗口和一个 `start_idx>0` 随机窗口，形成 50/50 样本池。train 的随机起点每次读取时重采样；val 的随机起点由 split seed 固定一次，确保 early stopping 各轮严格可比。
- **rollout：** 单帧预测，最长 20 帧；每步把预测帧 detach 后滑入 5 帧输入窗，后续 `start_vel=current[:,1]-current[:,0]`，与现有 single-frame eval 一致。每帧立即反传，不保存跨帧 BPTT 图。
- **loss：** `mean(all frame MSE) + 0.5 * mean(last-third MSE) + 1e-3 * mean((g-1)^2)`；正则只作用于当前材质 row。
- **优化：** 每种材质先在 `update=0` 评估并登记 identity gate 作为可回退基准；之后使用独立 Adam，`lr=3e-3`、无 weight decay/scheduler，最多 200 updates，25 updates 验证一次，连续 3 次不改善停止。只有优于 identity 的 row 才会取代它；三个材质分别恢复各自最佳 row，最终合并为一个 checkpoint。
- **参数量：** B3a `16,129,543`；B3b `16,129,555`；精确增加 12 个参数。
- **评测 config：** `src/configs/eval_mm3_b3b_material_stage_gate_screen.yaml`；calibration 完成并冻结后，只允许在 dev-test 上运行一次标准 eval，不根据结果继续调整 `lr/lambda/gate_max`。
- **成功标准：** plasticine long MSE 相对 B3a 改善至少 25%，且进入独立 baseline +10% 以内；elastic 不超过独立 baseline +10%；sand 相对 B3a 恶化不超过 5%；overall long/FDE 与地面穿透不恶化；gate 不大面积逼近 0 或 2。
- **停止条件：** 任一主材质越过退化线、gate 广泛饱和，或成功只在 dev-test 后调参才成立，则关闭 B3 adapter 家族，不继续扩展 rank/expert/stage。
- **calibration 结果：** elastic / plasticine / sand 的 best update 分别为 `25 / 75 / 0`；validation score 相对 identity 分别改善约 `0.0035% / 0.410% / 0%`。最终 gate 为：

  ```text
  elastic:    [0.9955, 1.0001, 0.9982, 0.9979]
  plasticine: [1.0013, 0.9945, 0.9990, 0.9991]
  sand:       [1.0000, 1.0000, 1.0000, 1.0000]
  ```

  所有 gate 均停留在 identity 附近；sand 明确恢复 `update=0` 的 identity row。正则项在该偏差范围内约为 `1e-8` 量级，不足以解释近 identity 结果。
- **dev-test 结果：** B3b 与 B3a 几乎逐项重合：overall full-rollout `2.516613e-3`（相对 B3a `-0.09%`），long `6.378690e-3`（`-0.08%`），FDE `9.875238e-2`（`-0.02%`），穿透率 `5.343%`（B3a `5.342%`）。
- **分材质 long MSE：** elastic `7.873908e-3`（相对 B3a `-0.18%`、相对独立 baseline `+13.5%`）；plasticine `8.560660e-4`（相对 B3a `-0.08%`、相对独立 baseline `+58.6%`）；sand `1.051290e-2`（与 B3a 相同、相对独立 baseline `-45.0%`）。
- **分析：** 训练后只调整每材质、每 stage 的标量幅度不能修复 B3a。现有证据更符合“共享 adapter 的更新方向或其与主干的 co-adaptation 不适合所有材质”，而非单纯幅度失衡。该结果不证明所有材料相关机制无效，但否定了 post-hoc scalar calibration。
- **当前状态：** **预注册判定 `close`。** Plasticine 未达到相对 B3a 改善 25% 和 baseline `+10%` 以内的标准，elastic 仍超过 baseline `+10%`；按停止条件关闭 B3 adapter 家族，不继续调 `lr`、正则、gate 范围、rank、expert 或注入 stage。
- **下一步：** 回到冻结的 `mm3_contact_cond@90k` 独立 baseline。下一项完整训练必须重新预注册，不从 B3a/B3b checkpoint 续接，也不能把 B3 的共享残差 adapter 换名后继续。

服务器命令（从 `src/` 运行）：

```bash
accelerate launch \
  --config_file configs/acc/1gpu.yaml \
  train_material_stage_gates.py \
  --config configs/config_mm3_b3b_material_stage_gate_screen.yaml
```

calibration 完成并检查产物后，dev-test 只运行一次：

```bash
python eval.py --config configs/eval_mm3_b3b_material_stage_gate_screen.yaml
```

### 9.2 B4 Continuous Material AdaLN 45k Screening

- **日期：** 2026-08-07 至 2026-08-08。
- **服务目标：** A / B / C / D，其中 C 为主要目标，A/B/D 为不可违反的安全门槛。
- **研究问题：** 在保留现有连续 `E/nu` token、`mat_type` class 路径和 contact 条件的前提下，直接以连续材料值调制每层 AdaLN，能否提高 `E/nu` 对动力学算子的控制权，同时保持三种材质的长程 rollout 安全？
- **机制假设：** 现有 `mat_type` 可直接进入 block conditioning，而连续 `E/nu` 主要经 condition token 间接读取。新增共享 `ContinuousMaterialConditioner: Linear(2,64) -> SiLU -> zero-init Linear(64,256)`，将 `[log10(E), nu]` 的 embedding 加到 timestep/class conditioning，使每个已有 `CogVideoXLayerNormZero` 以层内既有参数学习不同调制。
- **唯一变量：** 相对 `mm3_contact_cond` 的 8 层串行 baseline，仅新增连续材料 AdaLN 路径；保留原有 `E/nu` token、`mat_type`、gravity、floor、force、contact、loss、optimizer、batch、学习率、scheduler、数据、随机窗口和 point sampling。
- **参数审计：** baseline `16,095,491`；B4 `16,112,323`；仅一个 `ContinuousMaterialConditioner`，精确增加 `+16,832` 参数；仍为 8 个原始 `SpatialTemporalTransformerBlock`。
- **设计 / 实现 commit：** 设计 `436b564`；Task 1 conditioner `211cdcb`；Task 2 模型集成 `6206e87`；Task 3 配置 `fe72a21`；Task 3 精确值审计修复 `4eeb711`；Task 4 参数审计与正式登记 `c86be62`；最终审查修复 `92fafbb`。
- **训练 config：** `src/configs/config_mm3_b4_material_adaln.yaml`。
- **评测 config：** `src/configs/eval_mm3_b4_material_adaln_45k.yaml`（ON）与 `src/configs/eval_mm3_b4_material_adaln_45k_off.yaml`（OFF）。ON/OFF 使用同一 checkpoint，OFF 仅设置 `material_adaln_runtime_scale: 0.0`，不是独立训练 baseline。
- **训练预算：** 从头训练 `seed=0`，`max_train_steps: 90000`，`stop_after_steps: 45000`；比较对象为 matched-step `mm3_contact_cond@45k`。
- **Gate A，精度安全：** 相对 baseline@45k，overall full-rollout MSE、long MSE、FDE 均不得恶化超过 5%；elastic / plasticine / sand 各自 long MSE、FDE 均不得恶化超过 10%；first-chunk MSE 不得恶化超过 10%；地面穿透率增加不得超过 0.25 个百分点；不得出现任何材质明显视觉失效。
- **Gate B，连续材料控制：** Gate A 通过后，对 baseline@45k 与 B4@45k 运行相同 B2 sweep。`ignored` 比例必须至少降低 15 个百分点；提升不能只来自 sand，elastic 和 plasticine 均须出现超过 2% effect-size 的模型；`E/nu` condition-distance 与 response-strength 的关系不得变弱；sand 的既有 `E-shape` 单调证据不得明显下降；`responsive_non_monotonic + unstable_excessive` 不得超过 15%；`nu` 至少在两种材质上呈现可辨识的连续响应。由于没有 counterfactual GT，该门只判断 causal authority 与响应连续性，不声称反事实物理正确。
- **Gate C，路径承重诊断：** 对同一 checkpoint 比较 ON/OFF；两者预测必须存在稳定差异，OFF 后材料响应应下降，且 ON 相对 OFF 的变化不能仅由少数 sand 长尾样本驱动。该诊断只证明新路径被使用，不替代 Gate A/B。
- **停止条件：** Gate A/B/C 任一失败即记录负结果并停止 B4；不运行材料 sweep 后的 hidden dim、注入层数或学习率调参，不继续 90k，也不回到已关闭的 B3 adapter 家族。只有三项 Gate 全部通过，才从 B4@45k checkpoint 继续到 90k，且仅改变停止点。
- **评测协议：** `seed=0`；ON/OFF 均使用 `outputs/mm3_b4_material_adaln_8L/checkpoint-45000/model.safetensors`；164 个窗口、41 个 full-horizon model，其中 elastic/plasticine/sand 分别为 13/14/14。matched-step baseline 为 `mm3_contact_cond@45k`。
- **Gate A 结果：** overall full-rollout `4.452946e-3`（baseline `4.659667e-3`，`-4.44%`）、long `1.122776e-2`（`-7.19%`）、FDE `1.416000e-1`（`+1.84%`）、first-chunk `3.833849e-5`（`+5.37%`），四项 overall 门槛通过；但地面穿透率由 `1.222%` 升至 `3.244%`（`+2.022 pp`，超过 `+0.25 pp` 上限）。分材质 long：elastic `1.658744e-2`（`-0.04%`），plasticine `3.245096e-3`（`+294.43%`），sand `1.423358e-2`（`-25.85%`）；分材质 FDE：elastic `-12.67%`、plasticine `+110.28%`、sand `-9.64%`。Plasticine 明确越过 10% 退化线，因此 Gate A 失败。
- **物理诊断：** 相对 baseline，B4 ON 的 `vMSE`、`aMSE` 和体积相对误差分别恶化 `22.67% / 74.65% / 16.31%`。Plasticine free-space MSE 从 `6.310678e-4` 增至 `2.249962e-3`，说明退化不只来自地面接触，而是整体动力学发生偏移。
- **Gate C 结果：** 同 checkpoint 下，ON 相对 OFF 的 full-rollout、long、FDE 分别改善 `43.94% / 43.74% / 22.21%`；elastic/plasticine/sand long 分别改善 `34.35% / 20.29% / 53.96%`。这证明新增路径在三种材质上均被模型使用并承重，但 OFF 是训练后 knockout，不能证明连续 `E/nu` 响应正确，也不能替代独立 baseline。
- **Gate B 状态：** 未执行。Gate A 已触发预注册停止条件，因此不运行 B2 material sweep，避免在失败臂上继续调参或读取更多 dev-test 信号。
- **分析：** 共享连续 AdaLN 调制把较强控制权注入全部 8 层，但收益发生明显材质间重分配：sand 大幅改善、elastic 基本持平、plasticine 严重退化。Overall 改善主要由 sand 拉动，不能据此判定多材质方案成功。现有证据支持“共享调制方向与不同本构动力学存在冲突”，但尚不能证明冲突来自 `E/nu` 数值范围、类别相关捷径或具体 AdaLN 子路径。
- **当前状态：** **预注册判定 `close`。** 不继续至 90k，不运行 Gate B sweep，不调 hidden dim、注入层数或学习率；后续新机制必须回到独立 `mm3_contact_cond` baseline，并显式避免用同一共享更新方向强制统一三种材料。

### 9.3 非主线可选诊断

- B2.1：把 B2 扫描协议应用于已有 `v11a_contact_cond@90k` checkpoint。该实验不需要训练，但只能回答“旧 HST 是否改变材料响应”，不能解决 B2 暴露的问题，也不作为 B3 的前置条件。
- 只有当 B3 设计需要区分“状态反馈本身”与“材料-状态交互”时才执行 B2.1；否则跳过，避免回到已关闭的 v11a 路线。

## 10. 单项实验记录模板

### `<实验名称>`

- **日期：**
- **服务目标：** A / B / C / D
- **研究问题：**
- **机制假设：**
- **唯一变量：**
- **冻结项：**
- **代码 commit：**
- **训练 config / checkpoint：**
- **评测 config / split / seed / n：**
- **成功标准：**
- **停止条件：**
- **结果：**
- **分材质结果：**
- **物理合理性：**
- **分析：**
- **结论：** proceed / revise / close
- **下一步：**
